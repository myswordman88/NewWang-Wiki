# 【新王觉醒】官网现状盘点与后续更新规划（基线 v1.9.2）

> 盘点时间：2026-08-21 ｜ 站点 43 个 HTML 页 / 7 大导航板块
> 说明：本规划基于实际文件勘察（非仅凭记忆），数据驱动页内容在 `js/*-items.js`，由 `_build_*.py` 从 wolai 源文档烘焙。

---

## 一、现状盘点（按导航板块）

| 板块 | 子项 | 状态 |
|------|------|------|
| **模组概览** | 特色简介 / 作者简介 / 赞助详情 / 下载地址 / 更新日志 / 讨论社群 | ✅ 基本完成（changelog 为真实版本卡片页） |
| **新手专区** | 安装指南 / 常见问题 | ✅ 完成 |
| | 新手入门 `beginner.html` | ⛔ 占位（"正在完善中"） |
| **全新系统** | 基础改动 / 觉醒系统 | ✅ 完成（381 / 439 行长文） |
| | 转职系统 `changes-class.html` | ⛔ 占位 |
| | 合成配方 `changes-recipe.html` | ✅ 完成 |
| | 新增物品 `items-new.html` | ⛔ 占位 |
| **职业技能** | 技能总览 / 女巫 / 亚马逊 / 死灵法师 | ✅ 完成（死灵仅**召唤系+毒骨系**已接，巫术系缺失） |
| | 圣骑士 / 野蛮人 / 德鲁伊 / 刺客 / 术士 | ⛔ 占位（5 个，均无数据文件） |
| **装备详情** | 基底 / 魔法&稀有 / 套装 / 符文之语 / 暗金 / 手工 / 传奇&混沌 | ✅ 7 个合并页均已完成并挂载数据 |
| | 装备系统 `equipment-system.html` | ⛔ 占位 |
| | 赞助装备 / 稀有装备 / 混沌装备（独立文件） | ⚠️ **孤儿页**：内容已并入"魔法&稀有""传奇&混沌"，这 3 个旧文件未删、也未在导航中 |
| **装备打造** | 强化卷轴 / 锻造系统 / 重铸与无形化 | ✅ 完成 |
| | 符文共鸣 `crafting-rune.html` | ⛔ 占位 |
| | 封印词条 `crafting-seal.html` | ⛔ 占位 |
| **玩法构筑** | BD展示 `bd-list.html` | ⚠️ 仅 47 行外壳，待填充示例 BD |
| | BD构建 `bd-build.html` | ✅ 完成（1695 行，全站最复杂模块） |
| | 我的BD `bd-my.html` | ⚠️ 依赖 Supabase 登录/持久化后端，**后端未搭建** |

**数据文件覆盖**：`base / set / unique / runewords / crafted / legend / magic-rare / sorceress / amazon / necro` 均有 JS 数据；`assassin/barbarian/druid/paladin/warlock` 无。

---

## 二、核心不完善点（按风险/影响）

1. **导航死链与孤儿页（高可见度，低成本修）**
   - （已处理）`equipment-rare.html`、`equipment-chaos.html`、`equipment-donate.html` 三个孤儿页已于 2026-08-21 删除（内容已并入"魔法&稀有"/"传奇&混沌"/"赞助详情"，git 仍可 `git checkout` 还原）。
   - 建议在导航或合并页内做 301 式引导，或直接删除/合并，避免"幽灵页"。

2. **占位页集中在"内容深度"板块（影响专业度）**
   - 新手入门、转职系统、新增物品、装备系统、符文共鸣、封印词条、5 个职业页共 **13 个占位页**，占全站 30%。

3. **死灵法师巫术系未接入**
   - `_build_necro_skills.py` 的 `SECTIONS` 只含 召唤系+毒骨系，巫术系段落未解析 → 死灵页缺一块内容。

4. **"我的BD" 后端是硬阻塞（外部依赖）**
   - 页面依赖 `shared.js` 自动引导加载的 Supabase（`config.js` / `supabase-client.js` / `auth.js`）。
   - 需用户决策：是否启用 Supabase 云端持久化？还是先用 `localStorage` 本地草稿？否则该页只能做登录壳。

5. **BD构建器复杂度高**
   - `bd-build.html` 1695 行 + `bd-equip-picker.js` 等，是全站最大模块，建议做一次交互/数据回流回归测试。

---

## 三、建议更新顺序（分阶段，每阶段结束可备份一版）

### Phase 0 — 清理与一致性（可立即做，1 天内）
- [ ] 处理 3 个孤儿页：删除 `equipment-rare/chaos/donate.html` 或在合并页加"已并入"提示（同时清理导航若有残留）。
- [ ] 死灵法师巫术系接入：在 `_build_necro_skills.py` 的 `SECTIONS` 追加一行并重跑脚本（源文档同目录已有对应段落）。
- [ ] 全站一致性巡检：kicker 编号、卡片 HUD/悬停、页脚版本徽章、占位页"返回首页"按钮是否齐备。

### Phase 1 — 补齐导航可见占位页（中优先级，均可在 wolai 找到源）
按"影响导航完整度"排序：
1. `equipment-system.html`（装备系统总览/机制说明）
2. `crafting-rune.html`（符文共鸣）
3. `crafting-seal.html`（封印词条）
4. `changes-class.html`（转职系统）
5. `items-new.html`（新增物品）
> 做法：仿照已有页（文本页用 `.feature-*` 模板；若有配方/物品则新建数据 JS + 卡片页）。

### Phase 2 — 职业技能扩编（最大工作量，内容核心）
- [ ] 先补完**死灵法师巫术系**（同 Phase 0 脚本，低成本）。
- [ ] 再选 1 个职业做**完整模板**（推荐圣骑士或亚马逊之外的，按源文档完整度选）：新建 `_build_<class>_skills.py` + `js/<class>-skills.js` + `skills-<class>.html`，复用 `sorceress-db.js` 渲染引擎。
- [ ] 模板跑通后，**批量复制**其余职业（野蛮人/德鲁伊/刺客/术士）。
> 关键：保持职业技能页统一**简体中文**（构建脚本已内置 `zhconv` 繁→简）。

### Phase 3 — 玩法构筑后端（需外部决策）
- [ ] 决策"我的BD"持久化方案：
  - A. Supabase 云端：建项目 → 填 `config.js` 凭证 → 设计数据表（BD/装备/护符网格）→ `bd-my.js` 联调；
  - B. 本地 `localStorage` 草稿：零依赖，先上线可用版本，后续可迁云。
- [ ] `bd-list.html`（BD展示）填充示例 BD 或接入数据。
- [ ] BD构建器回归测试（拖拽吸附、护符网格、导出/导入）。

### Phase 4 — 质量与上线
- [ ] 响应式 + 跨浏览器回归（移动端导航下拉、卡片换行）。
- [ ] 部署校验：Vercel + OSS 双通道；确认 `assets/` 成品 webp 进部署、源/构建目录被排除（防裂图坑）。
- [ ] SEO meta、不蒜子计数、社交分享图。

---

## 四、关键建议

1. **先清死链，再补内容**：13 个占位页里，孤儿页和导航可见占位会直接暴露"未完成感"，Phase 0 成本最低、体验提升最明显。
2. **职业技能是内容核心也是最大坑**：务必复用 `sorceress-db.js` 引擎 + `_build_*_skills.py` 流水线，做 1 个标准职业模板后批量复制，避免每职业重复造轮子。
3. **"我的BD"后端尽早拍板**：它是唯一外部依赖项，Supabase 凭证/表结构不定，该页无法真正闭环；若暂时不想上云，先用 localStorage 出可用版。
4. **每个阶段结束打一个版本标签**（如 v1.9.3 → v1.10.0 → …），保持"备份=打标签"的既有纪律。
5. **新增职业/系统页时对齐既有约定**：kicker 编号（X.Y.Z）、卡片三栏、配色（固定词条 #6c6cff、变量金色）、简体中文，减少后续返工。

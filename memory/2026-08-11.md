# 2026-08-11 工作日志

## 创建「新王觉醒」官网（响应式静态站点）
- 需求：用户要求创建标题为【新王觉醒】官网的响应式页面，并指导部署上线。
- 交付物（位于工作区根目录）：
  - `index.html`：单页落地页，含导航/Hero/世界观/核心特性/纪元数据/最新动态/CTA/页脚；含完整 SEO、Open Graph、无障碍（skip-link、aria、焦点样式、prefers-reduced-motion）。
  - `css/style.css`：移动优先响应式，暗色+金色“觉醒”主题，CSS 变量、Grid/Flex、媒体查询、汉堡菜单、滚动动画。
  - `js/main.js`：移动端菜单、IntersectionObserver 滚动揭示、数字滚动计数、表单前端校验、页脚年份、平滑滚动。零依赖、零构建。
  - `DEPLOYMENT.md`：部署上线指南，覆盖 Vercel / Netlify / Cloudflare Pages / GitHub Pages / 腾讯云 EdgeOne 五种方案 + 上线验证与回滚清单。
- 验证：本地 `python -m http.server 8080` 启动，`/`、`/css/style.css`、`/js/main.js` 均返回 200。
- 说明：内容为占位示例，文案/数据/域名需用户替换为真实信息；表单仅前端演示，不发送数据。

## 按 MaxRoll.gg 风格重制样式
- 用户给网址 https://maxroll.gg/d2/guides/sorceress-leveling，要求主页改为此站风格。
- 已重写 `css/style.css`：由“金色史诗风”改为 MaxRoll 的“深色硬核游戏指南站”调性——近黑蓝调背景(#0c0d12)、青蓝强调色(#3da9fc/#2f6bff)、白字高对比、细边框+小圆角卡片、章节标题底部渐变强调线、左侧 accent 竖线的动态卡片、芯片徽章日期；保留全部 HTML 结构与 JS 交互。HTML/JS 未改动。

## 调整为「暗黑奇幻」游戏风格
- 用户反馈上一版偏商务，要求更暗黑奇幻、字体更游戏感、按钮更游戏化、去掉背景网格。
- `index.html`：新增 Google Fonts（Cinzel / Noto Serif SC 中文衬线 / Rajdhani），移除 hero 的 `grid-overlay` 网格 div。
- `css/style.css` 重写：配色改为近黑暖调底 + 余烬红(#e0533f/#9c241c)主色 + 古铜金(#c9a14a)强调 + 紫雾；标题用衬线中文(Noto Serif SC)+刻印阴影，标签/按钮用 Rajdhani 大写间距；按钮改为“金属铭牌”质感（红金渐变+古铜描边+内外阴影+悬停提亮）；卡片切角+HUD 边角保留并改红铜色；背景仅留光球+旋转能量晕，网格已去除。

## 按用户截图拆分为 12 大板块 + 下拉导航
- 时间：2026-08-12
- 需求：用户要求去掉主页光晕，并把板块细分为截图中的 12 类；顶部导航匹配，鼠标移到「模组概览」显示下拉子列表。
- 已完成：
  - `index.html`：重写为 12 大板块（模组概览 / 新手专区 / 更新日志 / 全新改动 / 职业技能 / 装备详情 / 装备打造 / 物品和配方 / 怪物和盟友 / 地图关卡 / 攻略心得 / 其他资源），每个板块下含对应子项卡片；主导航实现带 `aria-haspopup` / `aria-expanded` 的下拉结构。
  - `css/style.css`：移除 Hero 光晕、浮动光球与旋转 conic 渐变；保留暗黑奇幻配色与金属铭牌按钮；新增 `.dropdown` 桌面悬停下拉与移动端折叠子菜单样式；补充 `.cards-2` / `.cards-4` 网格；增加 `--crimson` / `--gold-soft` 变量以修复表单消息颜色。
  - `js/main.js`：为下拉菜单补充桌面 hover/focus 的 aria-expanded 联动，以及移动端点击父级展开/收起子菜单、关闭菜单时重置子菜单状态。
- 验证：本地 `http://127.0.0.1:8080` 返回 200，页面可正常访问。

## 板块结构二次调整（2026-08-12 晚）
- 用户要求保持配色字体不变，精简板块：
  - 删除独立「更新日志」顶级板块，将其（历史改动/完整规划/制作进度）并入「模组概览」作为子区块（id 仍 #changelog，位于 Mod 讨论社群卡片之后）。
  - 导航「模组概览」下拉顺序：特色简介、作者简介、赞助详情、下载地址、**更新日志**、Mod 讨论社群。
  - 「全新改动」板块更名为「模组特色」（id 保持 #changes）。
  - 删除「怪物和盟友」「地图关卡」「攻略心得」「其他资源」四个顶级板块（含导航项与对应 section）。
  - Hero 上方英文小字由 "AWAKENING · 觉醒纪元" 改为 "NEWWANG · 隔壁大王"。
- 文件变动：index.html（导航/板块重组）、css/style.css（新增 .subsection / .subsection-title 子区块样式）。js 未改。
- 预览：本地 8080 端口被旧会话残留进程占用，改用 8090 端口 `python -m http.server 8090` 正常（/、/css、/js 均 200）。

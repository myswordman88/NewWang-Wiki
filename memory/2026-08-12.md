# 2026-08-12 工作日志

## 新王觉醒官网（暗黑奇幻风格）
- Hero 区「当前版本/最后更新/即将上线」标签（`.hero-meta dt`）字体由 0.72rem 调大到 0.95rem，加 `font-weight:700`、字距收至 0.14em（css/style.css）。
- 新增「上线倒计时」模块：终点 `2026-08-29 09:00` 本地时间，置于 Hero 区 hero-meta 之后。
  - HTML：`<div class="countdown">` + 天/时/分/秒 四单元（#cdDays/#cdHours/#cdMins/#cdSecs）。
  - CSS：`css/style.css` 新增 `.countdown / .countdown-label / .countdown-timer / .cd-unit / .cd-num / .cd-cap / .cd-sep`，风格延续暗黑奇幻（古铜金 label、余烬红数字光晕、HUD 切角）。
  - JS：`js/main.js` 末尾新增 `tickCountdown()`，`setInterval` 每秒刷新，终点到 0 后停。
- 配色/字体整体保持暗黑奇幻不变（余烬红 + 古铜金 + 紫雾；中文 Noto Serif SC、英文/标签 Rajdhani）。
- 预览：本地静态服务端口已被回收，重启于 `http://127.0.0.1:8100`（返回 200）。

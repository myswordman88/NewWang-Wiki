# 2026-08-15 工作日志
## 重构基底装备卡片为暗黑核横向布局（未备份）
- 用户参考暗黑核底材卡片截图，要求更紧凑并预留图片方框。
- 修改 `js/base-db.js`：`cardHTML` 改为横向结构：左侧 `.base-thumb`（88×88 图片框，内置占位首字 + `<img>` 占位，未来有图时加 `.has-img` 即可显示），右侧 `.base-card-body` 分上/中/下三行。
  - 上行：标题组（中文名、普通/扩展/精英 + 分类、EN、简、繁）+ 需求组（需要等级/需要力量）。
  - 中行：防御范围 + 平均防御、耐久度。
  - 下行：Qvl、最大孔数左右对齐。
- 修改 `css/style.css`：`.base-grid` 改为单列；`.base-card` flex-row；调整字体、间距、颜色；小屏幕（≤560px）下改为纵向堆叠。
- 「译名对照」按钮改为切换显示/隐藏 EN + 繁 行（默认显示，符合截图）。
- 本地预览已启动 `http://localhost:8088/equipment-special.html`。全部未提交（待"备份"）。
## 基底装备卡片再调整：4列纵向网格（未备份）
- 用户反馈单列太长，要求改为一行4个；并细化：简/繁名称并排一行（简繁相同也标记繁）、简繁行与属性行间加分割线、属性数字放大、Qlvl 放大、需要等级/需要力量数字淡绿（#8fe0a8）。
- `js/base-db.js`：cardHTML 改为纵向结构——顶部图框、中文名、普通·分类、简/繁并排行、<hr>分割线、2×2 属性网格（防御/耐久/需要等级/需要力量）、底部 Qlvl / 最大孔数（虚线分隔）。需要等级/力量加 `.base-req-num` 类。
- `css/style.css`：`.base-grid` 改 `repeat(4,1fr)`（980px→2列、520px→1列）；`.base-card` 改纵向；属性数字 `.base-stat-num` 1.02rem；`.base-req-num` 淡绿；`.base-qlvl strong` 1.08rem；清理旧 560px 横向适配。
- 预览 `http://localhost:8088/equipment-special.html`（server 后台运行）。未提交（待"备份"）。

## 备份 v1.3.5
- 提交 `d0d0194`，43 文件（1420 插入 / 528 删除）。标签 `v1.3.5`（链 v1.3.4→v1.3.5），纯本地仓库。
- 全站 41 处 `.version-badge` 由 v1.3.4 同步为 v1.3.5。工作树干净。
- 入库内容：基底装备卡片4列纵向网格重构 + 全站页脚统一(v1.3.4样式)。
## 装备卡片属性文字右移 + 图片放大（未备份）
- 用户要求：耐久度/需要力量等属性文字尽量向右移动（不换行）、物品图片再放大一点。
- `css/style.css`：`.base-stat` 加 `justify-content:flex-end; white-space:nowrap;`（整体右对齐且禁止换行）。`.base-thumb` 宽 `25%→30%`；有图卡片标题/副标题/简繁行 `padding-right:27%→33%` 防被放大后的小图遮挡。
- 图片大小手动控制：改 `.base-thumb{width:30%}`（百分比=相对卡片宽，正方形靠 aspect-ratio:1/1）；padding-right 同步略大于该值即可。未提交（待"备份"）。
## 装备卡片属性左右分组 + 底部数字缩小（未备份）
- 用户要求：防御/耐久度 放左侧左对齐，需要等级/需要力量 放右侧右对齐；底部 Qlvl/最大孔数 数字缩小1号并下移贴底。
- `js/base-db.js` cardHTML：`.base-stats` 内改包两层 `.base-stat-group`（`.base-stat-left` 含防御/耐久度、`.base-stat-right` 含需要等级/需要力量）。
- `css/style.css`：`.base-stats` 由 2×2 grid 改 flex `justify-content:space-between`；加 `.base-stat-group`(column) + `.base-stat-left`(align-flex-start) / `.base-stat-right`(align-flex-end)；`.base-stat` 去掉 `justify-content`。`.base-card-bot` 加 `margin-top:auto` 贴底；`.base-qlvl strong` 1.08→0.92rem、`.base-sockets strong` 1→0.85rem。未提交（待"备份"）。
## 全分类 sprite 转换 + 217 条全部配图（未备份）
- 用户把全部分类 sprite 放入 assets/sprite/：armor/belt/boot/circlet/glove/grimoires/helmet/pelt/shield/voodoo_head 共 109 个（含 .lowend 已跳过）。
- **关键修复**：原脚本高度取 b[8:10]（恒为196）导致非 196 高精灵被裁切/报错。实测高度在 b[12:14]；各分类实际高：belt 196×98、boot/circlet/glove/grimoire/helmet/pelt/voodoo_head 196×196、armor 196×294、shield 196×294 或 196×392（均为 196 宽、98 的整数倍行）。
- 改 `sprite_to_webp.py`：`h = struct.unpack("<H", b[12:14])[0]`。重跑转换 109 个（原 23 头盔被覆盖，新增 86），合计 19287KB→1420KB（压缩率 7.4%）。
- 重跑 `_build_base_data.py`：217 条基底装备（头盔73/铠甲60/盾牌39/手套15/靴子15/腰带15）**全部配图**（webp 存在即写 img 字段）。
- 注意：sprite 是多帧网格（2 列 × N 行，98px/格），图标含多视图；卡片图用 object-fit:contain 整体缩放居中显示（不裁切）。未提交（待"备份"）。
## 装备卡片改 3 列 + 字体放大 + 删除译名按钮（未备份）
- 用户要求：卡片加大到一行 3 个（保持现状长宽比）、无法完整显示的图缩放居中、删除【译名对照】按钮（简繁始终显示）、简繁上方插入英文名行、字体整体放大 1-2 号。
- `css/style.css`：`.base-grid` 改 `repeat(3,1fr)`（920px→2列、560px→1列）；各字体放大（name 1.05→1.2、subtitle 0.76→0.84、tier/cat 0.72→0.8、names-row 0.82→0.92、name-label 0.7→0.78、stat 0.78→0.88、stat-num 0.92→1.02、bot 0.8→0.88、qlvl 0.92→1.0、sockets-strong 0.85→0.95、thumb-placeholder 1.4→1.7）；新增 `.base-en-name`（斜体、has-img 时 padding-right:33%）。
- `js/base-db.js`：cardHTML 简繁上方加 `.base-en-name`(it.name_en)、去掉 `base-name-extra` 类；删除 toggle 相关变量与 `bind` 里点击处理、`state.showExtra`。`equipment-special.html` 删除 `#baseToggleEn` 按钮。未提交（待"备份"）。
## 装备图片框回退为右上角小图（按比例缩放，未备份）
- 用户纠正：图片框改回右上角小图，但框按图片原生比例缩放（不再统一正方形、也不再强制 196×196 大列）。规则：固定宽 114px，高 = 114 × (图高/图宽)；手套196×196→框114×114，盾牌196×392→框114×228。图片框与卡片边界留 0.55rem 距离。
- 实现：`.base-card` 改回 `flex-direction:column`；`.base-thumb` 绝对定位 `top/right:0.55rem; width:114px; aspect-ratio:1/1`（height:auto），`img{width:100%;height:100%;object-fit:contain;padding:0.15rem}`；`js/base-db.js` 给 `<img>` 加 `onload` 设 `thumb.style.aspectRatio=naturalWidth+'/'+naturalHeight`（JS 内单引号需 `\'` 转义）。`.base-card.has-img .base-card-body{ padding-right:124px }` 避让文字（覆盖 body 默认右 padding 0.8rem）。未提交（待"备份"）。
## 备份 v1.3.6
- 提交 `6907843`，52 文件（1148 插入 / 299 删除）。标签 `v1.3.6`（链 v1.3.5→v1.3.6），纯本地仓库、无 remote。
- 全站 41 处 `.version-badge` 由 v1.3.5 同步为 v1.3.6。工作树干净。
- 入库核心：全分类 sprite→webp 管线（修复 SpA1 高度解码 b[12:14]、递归跳过 .lowend、items.json 容错映射）、217 条基底装备全部配图；卡片改 3 列+字体放大；删译名按钮+英文名行；图片框右上角小图按图片原生比例缩放(宽114px)；右侧属性左移、分割线缩短不碰图框。sprite_to_webp.py 与 memory/ 副本首次入库（.workbuddy、assets 仍被 .gitignore 排除，故 webp 源图不进 git）。
## 装备卡片按图片高度分三类布局（未备份）
- 用户要求按图片源高度分类卡片布局：≤196（手套/鞋/腰带）→ 需要等级/需要力量/最大孔数贴卡片右边框（`.base-card-body` padding-right:0.4、顶部元素仍 padding-right:124 避让图框、`.base-stat-right`与`.base-card-bot` margin-right:0.4）、两根分割线延长（`.base-divider` margin-right:0、bot margin-right:0.4）；=294（衣服）→ 保持现状仅 `.base-card-bot` margin-right:0.7→0.4（最大孔数右移+第二根虚线延长）；=392（盾牌）→ 完全保持现状。
- `js/base-db.js` 给 `<img>` 的 onload 增加：按 `this.naturalHeight` 给卡片加 `img-short`(≤196)/`img-mid`(≤294)/`img-tall`(>294) 类，驱动上述 CSS。未提交（待"备份"）。
## 装备英文名改【英 XXX】+ 图片框垂直居中于卡片顶与第一根分割线（未备份）
- 英文名格式：`js/base-db.js` 的 `.base-en-name` 显示改为 `【英 ` + name_en + `】`（如【英 Leather Gloves】）；`css/style.css` `.base-en-name` 字号 `0.8→0.9rem`（放大1号）。
- 图片框居中：`img` 的 onload 内新增逻辑——按 `thumb.offsetHeight`（图框真实高）设 `.base-card-body` 的 paddingTop，使第一根分割线距卡片顶 = `0.55rem(8.8px) + 图框高 + 0.55rem`；即方图(114×114)时图片框恰好在"卡片顶边框"与"第一根分割线"正中间。两根分割线、之间文字、底部文字随之整体下移。方图/衣服/盾牌均按各自图片高度居中（统一逻辑）。JS 已通过 `node --check`。未提交（待"备份"）。
## 装备卡片三类约定 c1/c2/c3（未备份）
- 用户正式约定：卡片自上而下三区域，由两条分割线恰好分隔——[顶部]名称区(高=图片框高)、[中间]属性区(高=属性行数)、[底部]补充区(固定1行)。
- 命名：c1=方图(高≤196，如手套196×196)、c2=中图(高=294，如衣服196×294)、c3=长图(高=392，如盾牌196×392)。（注：用户口述用"192×192/192×294/192×384"近似，实际精灵原生为196宽、高196/294/392；显示框宽固定114px、高按比例。）
- `js/base-db.js` onload 类由 img-short/img-mid/img-tall 更名为 c1/c2/c3。c1 已完美不改；c2/c3 改为与 c1 同规则：`.base-card-body` padding-right:0.4rem、顶部元素(名称/副标题/英文名/简繁)仍 padding-right:124px 避让图框、`.base-divider` margin-right:0(两根线延长到右端)、`.base-stat-right`与`.base-card-bot` margin-right:0.4rem(需要等级/需要力量/最大孔数贴近右边框)。`css/style.css` 顶部加注释块记录此约定。JS 已通过 `node --check`，无 img-short 残留引用。未提交（待"备份"）。

# 2026-08-14 工作日志

## 全站表格样式统一（暗黑奇幻静态站 F:\Work_Test\WebSite\2026-08-11-22-00-10）
- 【转职系统】changes-class.html：仅放「暂未开放，敬请期待。」，页脚版本号同步 v1.0.8。
- 【合成配方】changes-recipe.html：依据 `assets/2.4合成配方（待补充）` 导出 HTML 制作，分 2.4.1 原版配方(一句说明) / 2.4.2 改动配方(3列对比表) / 2.4.3 新增配方(2列表，12行公式) 三节，复用 `.feature-subsection`/`.feature-table` 暗色表格。
- `.feature-table` 增加列分隔线（th/td 加 `border-right`，末列 `none`）。
- 表格文字全局居中：`.feature-table` 与 `.price-table` 的 th/td 改 `text-align:center; vertical-align:middle`（原先只作用于合成配方页的 `.recipe-page` 级规则，已提升为全站全局，并删除冗余页面级规则）。
- 合成配方页首表（改动配方，3列）首列窄(24%)、后两列宽：`.recipe-first th:first-child, .recipe-first td:first-child { width:24% }`，仅影响该页。
- 待办：备份打标签（建议 v1.0.9）。

## 职业技能板块 + 全站导航同步
- 首页 index.html 职业技能板块：4 张 `<article>` 卡改为可点击 `<a class="card card-link">`（复用 hover 上浮），首项「技能改动总览」→「技能总览」；新增 5 张职业卡（圣骑士/野蛮人/德鲁伊/刺客/术士），容器改 `cards-3`（3×3，含 900/600px 响应式）。
- css/style.css 新增 `.cards-3` 规则。
- 导航栏「职业技能」下拉首项全站同步为「技能总览」并补齐 9 项（含新增5职业，全部指向 skills-*.html）：index.html 改过；其余 23 个详情页各自带导航副本仍为旧「技能改动总览」且只有4项，用脚本批量整体替换为与 index 一致的 9 项下拉。
- 批量生成 9 个占位详情页 skills-overview/sorceress/amazon/necro/paladin/barbarian/druid/assassin/warlock.html（基于 changes-class.html 模板），正文暂为「暂未开放，敬请期待。」。
- 待办：备份打标签（建议 v1.0.9，含以上全部未提交改动）。
- **已备份**：`git tag v1.0.9`（提交 c1978e2），工作区干净。含职业技能板块+导航同步+合成配方/转职页+表格列线/居中+首列窄，以及清理 assets/ 参考源（导出网页、2.4合成配方目录已删；导出网页原 v1.0.8 纳入版本库，本次一并提交删除，需找回可执行 `git checkout v1.0.8 -- "assets/导出网页/"`）。

## 技能总览页（skills-overview.html）正式填充
- 参考源：`assets/5.1技能改动总览/cH8GaTbczpUUaLSmXzXmaw/5.1技能改动总览.html`（wolai 导出，纯文字+公式，无图）。用 python html.parser 提取正文（修复表格列解析、公式 \times→× 转可读）。
- 页面分 4 个 feature-subsection：5.1.1 技能关联词条（新增技能强化词缀，按 基础/灵气/状态/召唤 4 组列表 + 多重散射减益对照表 feature-table）、5.1.2「超凡技艺」机制、5.1.3 技能伤害计算（影响因素列表 + 冰霜球举例含公式）、5.1.4 召唤物伤害计算（物理/元素因素 + 无/有混沌词条两场景含公式）。
- 新增全局 CSS 类 `.formula`（等宽、绿系高亮、可横向滚动、pre-wrap）用于展示伤害计算公式；公式中 LaTeX \times→×、\%→%。
- 页脚版本号 v1.0.9。本页为 9 个职业技能占位页中首个正式填充（其余 8 职业仍占位）。
- 已提交 v1.2.0。

## 技能总览页章节标题样式修正（用户明确约定）
- 原先把 5.1.1~5.1.4 高级标题误写成 `<h3 class="feature-subsection-title">` 且包在 `.feature-subsection` 内。
- 修正：4 个高级标题提出包裹、改为 `<h3 class="feature-section-title">`，其子项（如 `1. 新增技能强化词缀`）保留 `<h4 class="feature-subsection-title">` 在 `.feature-subsection` 内。结构对齐 changes-basic.html / changes-awaken.html。
- **用户约定（已写入 MEMORY.md）**：高级编号小节标题一律用 `feature-section-title`（独立、不进 feature-subsection 包裹），子项才用 `feature-subsection-title`；今后所有资料页都照此操作。

## 技能总览页微调（同轮）
- 多重散射对照表补全为完整 9 列：表头「多重散射 | 1~9」，数据「伤害加成修正 | 90%/81%/75%/69%/64%/59%/56%/52%/50%」（原仅 +1/+9 两行），`feature-table` 自适应列宽不裁切。
- 高级标题间距：全局加 `.feature-section-title:not(:first-of-type){margin-top:3rem}`，即除页面首个高级标题外，其余与上一行文字保持约 2 行间距（约 3rem）。此规则全站 `.feature-section-title` 生效，首个因 `:first-of-type` 不受加。

## 修复高级标题下「双分割线」
- 现象：5.1.2/5.1.3/5.1.4 标题下方两条线（feature-section-title 自带 border-bottom + 紧跟的 feature-subsection 自带 border-top 叠加）。5.1.1 因 feature-subsection 是 :first-of-type 已去上边框，故仅一条。
- 修复：全局加 `.feature-section-title + .feature-subsection { border-top: none }`，去掉紧跟标题的 feature-subsection 冗余上边框，只留标题下边框作分隔线；顺带修好 changes-basic / changes-awaken 同类叠线。

## 全站页面编号重排（导航基准 X.Y.Z）
- 用户要求：页面标题编号统一为「导航顶级项位置·下拉子项位置·序列号」= X.Y.Z；页面 kicker 改为 `Name·X·Y`（中点无空格）。导航序：模组概览=1 / 新手专区=2 / 全新系统=3 / 职业技能=4（来自 index.html 的 01~08 区块，装备详情=5、装备打造=6、物品和配方=8）。
- 用脚本批量改 22 个详情页 kicker（`模组概览 · 01`→`模组概览·1·1` 等）与内容章节：changes-basic 4.1→3.1、changes-awaken 4.2→3.2、changes-recipe 2.4→3.4、skills-overview 5.1→4.1；guide 第一步/二/三→2.1.2/3/4；faq「1. 闪退问题」→「2.2.1 闪退问题」。模组概览/新手专区组内容本就是导航基准(1.x/2.x)，无需改。
- 结果：全站 22 详情页 kicker 均为 `Name·X·Y`，内容章节全部 X.Y.Z 一致；无旧编号残留（assets/ 源导出文件除外，本就不改）。
- 决策：index.html 首页 7 个区块 kicker（`01 · 模组概览` 等）未动——它们是父级区块标签，已含导航位，视觉风格不同于详情页；已向用户说明，若需统一可再改。
- 已提交 v1.2.0。

## 备份打标签 v1.1.0
- 本次"备份"把以上所有未提交改动（技能总览填充、全站编号重排、标题约定 feature-section-title/双线/间距、community/changelog 对调、合成配方标题白字大号、表格列线居中/首列表宽等）一并提交，commit 4e94f86，24 文件改动，工作树干净。
- 把 `assets/`（用户新上传的技能总览 wolai 导出参考源）加入 `.gitignore`，延续 v1.0.9「参考源不纳入版本库」约定；本地参考源仍保留，仅不进版本控制。
- 标签：`git tag -a v1.1.0`。当前 latest = v1.1.0（v1.0.1→v1.0.9→v1.1.0）。

## kicker 数字段加空格 + 职业页内容清空（待备份）
- kicker 类型2 数字段 `1·1`→`1 · 1`（全站 22 详情页，X·Y 中点点两侧加空格），数字仍放大 15%（`.kicker-num`）；首页 `01 | 模组概览` 类型无 `·` 不受影响。约定已更新 MEMORY.md。
- 【女巫】skills-sorceress.html 正文清空，仅留 wolai 链接 `https://www.wolai.com/saYXXCHS8rLzN7DkewShZn`（新标签页，rel=noopener）。
- 【亚马逊】skills-amazon.html → wolai 链接 `https://www.wolai.com/7Q26eK3dFoVsU24RL533xA`。
- 【死灵法师】skills-necro.html → wolai 链接 `https://www.wolai.com/nheNaenv4vdAku3CDmx22s`。
- 其余 5 职业（圣骑士/野蛮人/德鲁伊/刺客/术士）wolai 未做好：正文仅「本职业页面尚未制作，敬请期待。」，保留各职业风味 lead 不动。
- 当前职业技能 9 页状态：技能总览(skills-overview)已正式填充；女巫/亚马逊/死灵=wolai 链接页；圣骑士/野蛮人/德鲁伊/刺客/术士=占位"敬请期待"。
- **已备份**：`git tag -a v1.1.1`（commit 85344af），122 文件，工作树干净。含 kicker 空格 + 3 职业 wolai 链接 + 5 职业占位 + images/skills-sorceress/ 与 _build_sor.py 等脚本。latest = v1.1.1（v1.0.1→v1.0.9→v1.1.0→v1.1.1）。

## 装备详情板块大改（待备份）
- 首页 index.html 装备详情板块：原 3 张 `<article>` 卡（装备系统框架/赞助装备/「套装」品质装备）→ 10 张可点击 `<a class="card card-link">`（悬停上浮+点击链接，复用 `.cards .card:hover`）。
- 重命名：装备系统框架→**装备系统**、「套装」品质装备→**套装装备**；新增 7 张：特等/魔法/稀有/暗金/手工/传奇/混沌装备。顺序严格按用户给定（装备系统→赞助→特等→魔法→稀有→套装→暗金→手工→传奇→混沌）。
- 容器改 `cards-5`（css/style.css 新增，5列网格+1180/720/480px 响应式回退），10 卡排 2×5。
- 卡片链接指向新建 10 个详情页 equipment-{system,donate,special,magic,rare,set,unique,crafted,legend,chaos}.html。
- 全站 23 个现有 HTML 的「装备详情」导航下拉批量替换为 10 项（脚本 `_build_equip.py`）；10 个装备详情页各自导航高亮当前项 `<span class="current">`。
- 10 个装备详情页模板同 skills-paladin.html 占位页：kicker `装备详情 | 5 · N`（N=1~10）、面包屑 首页/装备详情/当前、正文「本装备页面尚未制作，敬请期待。」、页脚版本 v1.1.0。
- 编号约定写入 MEMORY.md：装备详情=5，子项 5.1~5.10 对应上述 10 页。
- **插入【符文之语】**（用户补单）：在套装装备(6)与暗金装备之间插入第 7 项，全站装备详情卡片/导航下拉变 11 项，编号顺延为 5.1~5.11（符文之语=5.7，暗金=5.8…混沌=5.11）。新建 equipment-rune.html（图标📜，正文占位"敬请期待"）。复跑 `_build_equip.py` 统一重建首页卡片(cards-5 现 11 张)、全站 33 页导航、11 个装备详情页（kicker 重排）。已校验：首页顺序、导航、rune 页 current 高亮、旧 5.10 编号无残留。MEMORY 装备详情子项列表同步更新为 11 项。
- 已提交 v1.2.0。

## 套装装备页正式填充（equipment-set.html，待备份）
- 参考源：`assets/6.3 「套装」品质装备/ftyuzXwkgg9NWsshbjK5Lk/6.3 「套装」品质装备.html`（wolai 导出，纯文字 + 3 列 wolai-row 网格，无图无表；用户要求忽略的"引用页面"在源中并不存在，故未渲染任何引用内容）。
- 章节按本站编号 5.6.x（源原为 6.2.x）：5.6.1「套装」改动总览（单段说明，已清理源里重复"随机词条 随机词条"）、5.6.2 套装随机词条一览（11 个部位：所有部位1/2、头盔、护甲、腰带、手套、鞋子、武器、盾牌、法杖、戒指+项链，每部位 = feature-subsection 包裹 h4 标题 + feature-list ul 词缀，每部位 8 条，共 88 条）。
- 转换脚本 `_build_set.py`：DOM 解析 wolai-row > wolai-col；标题取含 `bg-` 的块文本（修复「鞋子」列多一个空 `wolai-text` 块把标题覆盖成空串、导致该部位被跳过的问题）；词缀取 ul li 文本（丢弃 svg marker 与空 `jill` span）；`clean_affix` 还原 wolai 丢失的「数字+点/%」「%+中文」之间空格。
  - 校验通过：2 章节标题、11 部位、88 词缀 li、无空 p/li、无 wolai/inline-wrap/jill/marker/svg 残留；kicker 保持 `5 · 6`、h2 套装装备、lead 保留。
  - 已提交 v1.2.0。

## 装备打造板块大改（待备份）
- 首页 index.html 装备打造板块：删除【套装继承系统】卡片；【强化卷轴系统】卡改名为【强化卷轴】并升级为可点击 `<a class="card card-link" href="crafting-scroll.html">`（悬停上浮，其余 4 卡保持锚点 article）；【封印词条系统】卡改名为【封印词条】。现 5 张卡（强化卷轴/锻造/重铸/符文共鸣/封印词条）。
- 全站导航栏「装备打造」下拉同步：删除套装继承系统；强化卷轴系统→强化卷轴（href→crafting-scroll.html）；封印词条系统→封印词条。共 33 个 HTML 文件（index + 32 详情页）批量替换（脚本 `_build_crafting.py`，其中 crafting-scroll.html 由 equipment-set.html 模板生成）。
- 新建【强化卷轴】独立页 crafting-scroll.html：参考源 `assets/7.1 强化卷轴系统/scRS3qjhvqCj3xnb9aipP1/7.1 强化卷轴系统.html`（wolai 导出，标准 HTML <table> 含 4 种卷轴 × 武器/防甲/饰品 三列）。章节 6.1.x：6.1.1 系统简介（单段）、6.1.2 强化卷轴一览（4 个 feature-subsection，每个卷轴下按 武器/防甲/饰品 三组 feature-list 列出「属性 min~max（等级需求 n）」，共约 104 条）。
- 解析要点：table DOM 提取（thead 表头 + 带 bg- 类的子表头行切换卷轴类型；每行 16 个 td，按列索引 2-5/7-10/12-15 取 武器/防甲/饰品 三类的 属性/min/max/等级需求）；忽略每行"档位标记"列（normal/常规初级属性/无限制 等）。
- 校验：全站无"套装继承系统/封印词条系统"残留（仅 _build_*.py 脚本含删除逻辑字符串）；"强化卷轴系统"仅存于 crafting-scroll.html 章节标题 6.1.1 与 assets 源；首页卡片区与导航 current 高亮均正确。
- 已提交 v1.2.0。

## 强化卷轴页 6.1.2 改为表格（待备份）
- 用户要求：6.1.2「强化卷轴一览」由三组 feature-list 改为表格罗列，参考 assets 源文件表格。
- 改造 `_build_crafting.py`：解析时存结构化数据（属性/min/max/等级需求 元组），6.1.2 改为 4 张 `feature-table`（每种卷轴一张），表头「适用装备 | 强化属性 | 数值范围 | 等级需求」，数值范围 = min~max。
- 校验：4 表共 104 行（普通25/魔力28/稀有27/暗金24），与源一一对应；feature-list 残留 0；表头列名贴合源（源表头含"等级需求"列）。
- 已提交 v1.2.0。

## 强化卷轴页表格三项微调（待备份）
- 等级需求列数字前加 +（如 +4，表示物品需求等级 +4）。
- 【适用装备】"防甲"→"防具"（解析与显示统一为 武器/防具/饰品）。
- 武器↔防具、防具↔饰品 之间加粗金色分隔线：`.feature-table tbody tr.row-sep td { border-top: 2px solid #c9a14a }`，仅每组首行加 class="row-sep"（武器组首行不加）；4 表共 8 条分割线。
- 校验：row-sep 8 处、等级需求全带 +、全站无"防甲"残留（含 _build_crafting.py）。
- 已提交 v1.2.0。

## 强化卷轴页表格隔行变色（待备份）
- 用户要求：相邻两行背景色差异显示，方便阅读。
- 纯 CSS 改动（无需改 HTML/脚本）：`css/style.css` 加 `.feature-table tbody tr:nth-child(even) td { background: rgba(255, 215, 154, 0.06); }`，偶数行加极淡金色透明背景做斑马纹，奇数行保持 --surface，与暗黑奇幻主题协调且不干扰 row-sep 粗金色分隔线。
- 已提交 v1.2.0。

## 强化卷轴页武器↔防具分隔线改亮绿（待备份）
- 用户要求：武器和防具之间那条黄色醒目的分界线改成亮绿色。
- 区分两条分隔线：`_build_crafting.py` 中防具组首行（ci==1，武器↔防具）加 class="row-sep row-sep-green"，饰品组首行（ci==2，防具↔饰品）保持 class="row-sep"（金黄 #c9a14a）。CSS 新增 `.feature-table tbody tr.row-sep-green td { border-top: 2px solid #5dff8f }`。
- 校验：4 表共 4 处 row-sep-green（亮绿）、4 处 row-sep（金黄）；武器↔防具为亮绿、防具↔饰品为金黄。
- 已提交 v1.2.0。

## 强化卷轴页两条分隔线统一亮绿（待备份）
- 用户要求：防具↔饰品的分界线也应用亮绿色，与武器↔防具统一。
- `_build_crafting.py` 中分隔线判定由 `ci == 1` 改为 `ci > 0`（ci==1 防具组、ci==2 饰品组首行均加 class="row-sep row-sep-green"），ci==0 武器组首行不加。CSS 不变（row-sep-green = #5dff8f）。
- 校验：4 表共 8 行分隔线，全部为 row-sep row-sep-green（亮绿），无纯 row-sep 黄色残留。
- 已提交 v1.2.0。

## 强化卷轴页数值范围加空格（待备份）
- 用户要求：【数值范围】列 ~ 符号左右加空格（如 10~12 → 10 ~ 12）。
- `_build_crafting.py` 生成 tr 的格式串 `%s~%s` 改为 `%s ~ %s`（仅数值范围列，不影响等级需求 + 号、% 号）。
- 校验：数值范围列均为 `x ~ y` 形式（含带 % 的 `1% ~ 1%`）。
- 已提交 v1.2.0。

## 装备打造板块其余4卡链接化（v1.2.0 后，待备份）
- 锻造系统/重铸与无形化/符文共鸣/封印词条 4 张首页卡由锚点 `<article>` 改为 `<a class="card card-link" href="crafting-xxx.html">`，并新建 4 个占位页（crafting-forge/reforge/rune/seal.html，编号 6.2~6.5），全站 33 页导航下拉同步为链接。
- 脚本 `_build_crafting_pages.py`（生成占位页+同步）。
- 校验：首页 5 张卡均为 card-link 链接；全站 HTML 无 `#crafting-*` 锚点残留（仅脚本字符串无害）。

## 锻造系统页正式内容（v1.2.0 后，待备份）
- 源：`assets/7.2 锻造系统`（wolai 导出，本站编号 6.2.x；图片按用户要求忽略不入页）。
- `_build_forge.py` 解析 wolai `wolai-simple-table`→`wolai-te` 单元格（按列切片/序号锚点）+ `zhconv` 繁→简 + `html.escape` 转义 + `~` 加空格。
- 章节：6.2.1 简介（总述+锻造方法6步+传奇词条升级方法3步，feature-list）；6.2.2 稀有·锻造石一览（表 编号/锻造属性/消耗锻造潜能，20 行）；6.2.3 传奇·锻造石一览（表 序号/职业/技能/锻造石名称/锻造石效果/传奇装备/消耗锻造潜能/不同T级详细数据，24 行，T级数据按表内 T级块对应序号提取、`<br>` 分隔 T1-T5）；6.2.4 混沌词条一览（表 序号/词条名称/词条效果/不同T级详细数据/混沌装备/实装，5 行，混沌词条表错位故硬编码已核对源）。
- 解析坑：wolai 把「不同T级详细数据」做成可折叠，主表 te 流中该列多为空，T级数据需从表内 `T级详细数据（点击箭头展开）` 文本块单独提取并按文档顺序对应序号；传奇装备「碎星」(STAR BREAKER 缺右括号，脚本 fix_eq 补 )；table3 错位严重故硬编码 5 行。
- 复用 `.feature-table`（斑马纹/隔行/金色表头）。校验：全站无繁体残留、无占位文本残留、& 已转 &amp;。

## 备份打标签 v1.2.0
- 本次"备份"把 v1.1.1 之后的全部未提交改动一并提交：commit 45faf0b，39 文件改动(3105 插入/206 删除)；新增 _build_crafting.py/_build_equip.py/_build_set.py 脚本与 crafting-scroll.html + 11 个 equipment-*.html 详情页。
- 内容涵盖：kicker 竖线分隔符+放大、女巫/亚马逊/死灵转 wolai 链接、余 5 职业占位、装备详情 11 卡(插符文之语)、套装装备页正式内容、赞助装备链接页、装备打造板块(删套装继承/强化卷轴/封印词条改名/强化卷轴独立页表格化及分隔线/隔行/数值格式微调)。
- 标签：`git tag -a v1.2.0`。当前 latest = v1.2.0（v1.0.9→v1.1.0→v1.1.1→v1.2.0）。本地仓库，无 remote。

## 装备打造其余 4 卡链接化 + 占位页（待备份）
- 用户要求：装备打造板块另外 4 张卡（锻造/重铸与无形化/符文共鸣/封印词条）也做成与强化卷轴一致的悬停效果和链接。
- 用脚本 `_build_crafting_pages.py`：① 以 crafting-scroll.html 为模板生成 4 个占位页（crafting-forge/reforge/rune/seal.html），编号 6.2~6.5，正文"本打造页面尚未制作，敬请期待。"，section-lead 取卡片描述，nav 下拉锚点改链接；② index.html 4 张 `<article class="card">` 卡升级为 `<a class="card card-link" href="crafting-xxx.html">`（保留内部内容）；③ 全站根目录 33 个 .html 导航下拉装备打造 4 项 `#`/`index.html#` 锚点统一改为 crafting-xxx.html 链接。
- 校验：index.html 装备打造 5 卡均为 card-link 链接；全站 html 无 `#crafting-(forge|reforge|rune|seal)` 锚点残留（仅 _build_*.py 脚本含，无害）；4 占位页 kicker/标题/面包屑正确。

## 锻造系统 6.2.3/6.2.4 宽表美化（待备份）
- 用户反馈 6.2.3 表（8 列、多列长文本）单元格被压成两行、垂直居中难看。优化：① `css/style.css` 新增 `.table-scroll`（overflow-x:auto + 边框/圆角，窄屏横向滚动不挤压）、`.feature-table--wide td`(text-align:left; vertical-align:top; line-height:1.7)、`.feature-table--wide td.c-c`(短列保持居中)、`.tlvl`(T级标签金色加粗 nowrap)。② `_build_forge.py` 给 6.2.3/6.2.4 表加 `feature-table--wide` + 内联 `table-layout:fixed; min-width` + `<colgroup>` 列宽（6.2.3: 7/8/10/14/20/16/11/14%；6.2.4: 8/22/28/26/12/4%）+ 短列加 c-c；tdetail 给 `T\d（…）？：` 包 `<span class="tlvl">`。③ 两张表包进 `.table-scroll` 容器。
- 校验：6.2.3/6.2.4 长文本列左对齐顶对齐、T级标签金色、宽屏占满/窄屏滚动；6.2.2 短表保持原居中样式不受影响。
- 已备份 v1.3.0。

## 锻造系统 6.2.3 表突破框架+全部居中（待备份）
- 用户要求：只把 6.2.3 这一个表边界拓宽、超出内容栏框架（1200px），且表内文字横竖都居中。
- `css/style.css` 新增 `.table-breakout`（width:min(1400px,94vw); margin-left:50%; transform:translateX(-50%) 以视口为基准居中加宽、突破父容器 1200px）+ `.feature-table--center td`（text-align:center; vertical-align:middle; line-height:1.6）。
- `_build_forge.py` 仅 6.2.3 的容器改 `table-scroll table-breakout`、表改 `feature-table feature-table--center`；6.2.4 仍 `--wide`（左对齐顶对齐）不受影响，6.2.2 短表不变。
- 校验：6.2.3 容器为 table-breakout、表为 --center；6.2.4 仍为 --wide 未突破。
- 已备份 v1.3.0。

## 锻造系统 6.2.3 列宽压缩（待备份）
- 用户要求：前几列尽量窄（刚好不换行即可），最后一列（不同T级详细数据）尽量留宽。
- `_build_forge.py` 6.2.3 的 colgroup 由百分比改为固定 px（序号46/职业60/技能68/名称92/效果200/装备118/潜能82）+ 末列不设 width 自动占满剩余；表 min-width 由 980 提到 1080px。
- 效果：宽屏下最后一列约 700px+，T1~T5 各行基本单行；前 7 列紧凑不换行。
- 已备份 v1.3.0。

## 锻造系统 用户手动微调列宽 + 两处内容微调（待备份）
- 用户在渲染后手动微调 6.2.3 colgroup 列宽（最终 66/102/98/132/320/158/112px），并已同步回 `_build_forge.py`（避免重生成覆盖）。
- 微调1（直角引号）：全站可见内容弯引号 `""`（U+201C/U+201D）包裹的中文词条改为直角引号 `「」`。关键坑：`zhconv` 的 `z2s()` 会把 `「」` 转回简体弯引号 `""`，故必须在 `z2s` 之后套 `q()` 还原；6.2.2/6.2.3 早已套 q()，但 6.2.4（TABLE3 硬编码）漏套导致残留——已补 q()（name/eff/eq 字段）。
- 微调2（碎星换行）：「碎星」(STAR BREAKER) 英文单独成行。实现：`fix_eq` 先 `z2s`→`q`→`esc`，再在 `「…」（` 后插真实 `<br>`（注意 eqd 已在格式串里 esc 过，不能在 esc 之后再插 `<br>`，否则会被二次转义成 `&lt;br&gt;`）。
- 校验（重生成后）：HTML 无 `""` 残留、无 `&lt;br&gt;`；`「碎星」<br>( STAR BREAKER )` 正确；6.2.4 `「混沌·正义之怒」` 等已统一直角引号。
- 已备份 v1.3.0。

## 锻造系统 6.2.4 表格纵向居中（待备份）
- 用户反馈 6.2.4（混沌词条一览）表未全部居中（纵向）。根因：6.2.4 表用 `feature-table--wide`（td: vertical-align:top 顶对齐），与 6.2.3 的 `--center`（横竖居中）不一致。
- `_build_forge.py` 把 6.2.4 表 class 由 `feature-table--wide` 改为 `feature-table--center`（容器 .table-scroll、colgroup 6列百分比、table-layout:fixed 均保留，不突破框架）。重生成后 6.2.3/6.2.4 两表均为 `--center`，横竖居中一致。
- 已备份 v1.3.0。

## 重铸与无形化页正式内容（待备份）
- 源：`assets/7.3 重铸与无形化`（wolai 导出，本站编号 6.3.x；图片按用户要求忽略不入页；源内 banner/title/main-title/image 块均无视）。
- `_build_reforge.py` 解析 wolai-block 文本（按前缀定位块，非硬编码 index）+ `zhconv` 繁→简 + `q()` 还原直角引号 `「」`（z2s 会把 `「」` 打回弯引号，必须在 z2s 后套 q）+ `html.escape` 转义（源含 `<数值>` 须转 `&lt;数值&gt;`）。
- 章节：6.3.1 重铸系统（重铸简介段落 + 重铸公式表[底材品质/重铸配方/重铸结果，4行] + 暗金咒符提示 + 重铸材料段落+分解公式表[分解材料/产物，2行]）；6.3.2 装备无形化（无形化简介段落 + 无形化公式表[合成配方/转化几率/说明，3行：10/25/45%] + 箭袋提示 + 保底说明段落 + 无形化材料段落）。本页另 1 张 wolai-simple-table「词条显示/大保底额外奖励/BUFF效果演示」实际内容为空（演示图片），忽略。
- 公式文本为连写，脚本用正则按品质/数量切分（重铸 `1件任意 (魔法|稀有|套装|暗金)…珠宝\s*\+(\d+)「暗金之尘」`；分解 `(\d+)件任意 (暗金|套装)…+1 融冰药水…`；无形化 `\+(\d+)「英雄旗帜」=…(\d+)%`）。重铸材料段落用 `re.sub(r'「分解装备」.*$','')` 去掉粘连公式（公式已入表）。
- 表格用普通 `.feature-table`（默认横竖居中、斑马纹），每表 colgroup 定宽 + table-layout:fixed + .table-scroll 容器；section-lead 改为"重铸刷新属性、无形化提升基础——装备打造的两大核心机制。"。
- 校验：占位文本无残留、无弯引号残留、`&lt;数值&gt;` 正确、4/2/3 行公式齐全。
- 已备份 v1.3.0。

## 第 8 板块 物品和配方：合成配方/新增物品（待备份）
- 用户要求：① 第 8 板块两张卡（合成配方/新增物品）完成悬停+点击链接；② 参考 `assets/8.1 合成配方` 完成【合成配方】页；③ 【新增物品】写"尚未实装，敬请期待"。
- 编号约定：第 8 板块=物品和配方；合成配方=8.1（items-recipe.html，kicker `8 · 1`）、新增物品=8.2（items-new.html，kicker `8 · 2`）。
- `_build_recipe8.py` 解析 `assets/8.1 合成配方`（wolai 导出，2 张 wolai-simple-table）：表1=改动配方[配方内容/原版配方/Mod改动配方，3 行]，表2=新增配方[配方内容/配方公式，13 行有效（末尾 4 空行已过滤）]。生成 items-recipe.html 全文：8.1.1 原版配方（一句话"详情查询游戏内「凯恩之书」"）、8.1.2 改动配方（表 recipe-first）、8.1.3 新增配方（表 recipe-first）。图片按用户要求忽略。
- items-new.html 由 items-recipe.html 改 kicker/标题/面包屑/lead+占位正文"该内容尚未实装，敬请期待。"（同前次符文共鸣/封印词条占位文案）生成。
- index.html 两张卡 `items-recipe`/`items-new` 由 `<article class="card">` 升级为 `<a class="card card-link" href="items-recipe.html"/items-new.html">`（428/433 行）。
- 全站同步：脚本批量把 38 个 .html 导航下拉 `index.html#items-recipe`→`items-recipe.html`、`index.html#items-new`→`items-new.html`；仅 `_build_equip.py` 脚本字符串残留（无害）。
- 校验：全站无 `index.html#items-(recipe|new)` 锚点残留（html 文件）；items-recipe.html 表1=3/表2=13 行正确、无占位残留；两页 nav 下拉均指向 items-recipe.html/items-new.html。
- 已备份 v1.3.0。

## 备份打标签 v1.3.0
- 本次"备份"把 v1.2.0 之后的全部未提交改动一并提交：commit 7b7929d，57 文件改动(2744 插入/220 删除)。
- 内容涵盖：装备打造其余 4 卡链接化 + 占位页；锻造系统 6.2.x 正式页(含 6.2.3 突破框架/列宽压缩/全部居中、6.2.4 纵向量中、直角引号/碎星换行微调)；重铸与无形化 6.3.x 正式页；符文共鸣/封印词条占位页；第 8 板块合成配方/新增物品卡链接化 + items-recipe.html 正式页 + items-new.html 占位页；全站导航同步。
- 标签：`git tag v1.3.0`。当前 latest = v1.3.0（v1.0.9→v1.1.0→v1.1.1→v1.2.0→v1.3.0）。本地仓库，无 remote。
- 提示：一批临时调试脚本（`_probe*_forge.py`/`_probe*_recipe8.py`/`_explore*_forge.py`/`_explore*_reforge.py`/`_probe_forge.py`/`_probe_recipe8.py` 等）随 `git add -A` 一并入库，若需清理可后续删除。

## 主页页脚三处修改（待备份）
- 用户要求：① 主页 footer 导航链接（原 模组概览/新手专区/更新日志/装备打造 4 个站内锚点）改为「资料站链接 |」前缀 + 5 个外部链接：赞助链接(https://ifdian.net/a/bigwang)、新王觉醒/魔王降临/王者归来/Mod小站(4 个 wolai 链接)，均 target="_blank" rel="noopener"；② 版权文字 `© 2026 新王觉醒. 保留所有权利.` 改为 `By 隔壁大王`；③ 页脚版本徽章 `当前版本 v1.0.2` 改为 `网站版本 v1.3.0`（同步当前 git 最新 tag），并约定今后每次备份打标签时同步更新此徽章为最新版本号。
- `css/style.css` 新增 `.footer-links-label`（前缀文字 + `::after` 竖线分隔）。
- 待办：未提交（用户尚未说"备份"）；footer 徽章目前已与 v1.3.0 一致。

## 主页页脚样式微调（续，待备份）
- ① 页脚链接前缀 `.footer-links-label`「资料站链接」颜色由灰(--text-mute)改淡绿 `#8fe0a8`，并补 `font-family:var(--font-ui); text-transform:uppercase; line-height:1.2` 与右侧链接字号(0.95rem)/字距(0.04em)对齐；竖线 `::after` 颜色由 --border-strong 改暗红 `#9b2d2d`。
- ② `.footer-copy`「By 隔壁大王」字号 0.85rem→0.95rem，并补 `font-family:var(--font-ui); text-transform:uppercase; letter-spacing:0.04em` 与链接同字体同大小对齐。
- ③ 版本徽章文本 `网站版本 v1.3.0` → `网站版本 v&nbsp;&nbsp;1.3.0`（字母 V 与数字间加 2 个不换行空格；badge 含 text-transform:uppercase 故显示 V）。

## 主页页脚表格对齐（续，待备份）
- 为解决「资料站链接」前缀、各外链、「By 隔壁大王」、版本徽章在 flex 行内基线/高度不齐的问题，把页脚右侧一行整体改为**隐藏表格布局**：
  - `index.html`：用 `<div class="footer-row">` 包裹 `<nav class="footer-links">`、`<p class="footer-copy">`、`<p class="version-badge">`。
  - `css/style.css`：`.footer-row { display: table; border-collapse: collapse; }`；`.footer-row > .footer-links/.footer-copy/.version-badge { display: table-cell; vertical-align: middle; white-space: nowrap; }`；`.footer-links` 本身也设 `display: table`，其直接子元素（label + 5 个 a）均为 `display: table-cell; vertical-align: middle; padding: 0 0.7rem;`，确保所有文字在同一行严格垂直居中对齐。
- 保留了淡绿前缀、暗红竖线、版本徽章的 V 与数字双空格。

## 主页 CTA 反馈区改版（续，待备份）
- 原「加入」区（`#join` 的 email 预约表单）改为「反馈」区：删除 `label+input` 邮箱框与 `form`，标题「觉醒之夜，缺你不可」→「如果遇到BUG，请反馈给我」，副文案→「您的反馈和建议，可以让「新王觉醒」变得更好！」，`立即预约` 按钮→`反馈BUG` 外链 `<a class="btn btn-primary" target="_blank" rel="noopener">` 指向 `https://www.wolai.com/f/nsqmUC5qfWXtXrbFaZJqjN`。
- `js/main.js` 删除失效的「预约表单邮箱校验」死代码（原依赖 #joinForm/#email/#formMsg，已因表单移除而弃用）。

## 已备份 v1.3.1（2026-08-14）
- 提交 `bebabe0` / 标签 `v1.3.1`：主页页脚重构（资料站5外链 + 隐藏表格对齐 + 淡绿前缀/暗红竖线 + 链接组与版本框间距）+ CTA 反馈区（反馈BUG 外链 wolai）+ 清理预约表单死代码。
- 按约定，备份前已将 `index.html` 页脚 `.version-badge` 同步更新为 `网站版本 v  1.3.1`（保留 V 与数字双空格），与 git tag 一致。

## 清理调试脚本 + 备份 v1.3.2（2026-08-14）
- 经用户确认，用 `git rm` 从仓库彻底删除 15 个开发期调试/探索脚本：`_probe_forge.py`/`_probe2~7_forge.py`(7)、`_explore_forge.py`、`_explore_reforge.py`、`_explore_recipe8.py`、`_probe_recipe8.py`、`_dump.py`、`_parse_sor.py`、`_parse_sor2.py`、`_fig_per_skill.py`（共 917 行删除）。
- 保留 10 个正式脚本：`_build_crafting.py`/`_build_forge.py`/`_build_reforge.py`/`_build_recipe8.py`/`_build_crafting_pages.py`/`_build_sor.py`/`_build_equip.py`/`_build_set.py`（页面生成器）+ `tools/optimize_images.py`/`tools/optimize_to_target.py`（图片工具）。
- 提交 `01bf42b` / 标签 `v1.3.2`；页脚版本徽章同步为 `网站版本 v  1.3.2`。

## 部署前核查（用户手动删文件，未提交）
- 用户手动删除 `images/skills-sorceress/`（150+ 张术士技能图）与 `assets/` 内的参考网页（直接删工作区，未 git rm）。
- 核查结论：全站 HTML/CSS/JS 对 `images/`、`assets/`、`*.png/*.jpg/*.svg` 零引用，删除对正式页面无影响、线上不会断图；主页仅依赖 `./css/style.css` + Google Fonts 外链，无 favicon/manifest 强制依赖。
- `git status` 显示 `images/skills-sorceress/*` 为 `D`（未提交）；若部署基于 git 仓库需先提交删除。纯静态站，部署需静态托管 + 外网访问 wolai/ifdian/Google Fonts。
- 已执行：commit `49be981` / 标签 `v1.3.3`，提交手动删除的 `images/skills-sorceress/`（93 张术士技能图）与 `assets/` 参考网页（`git ls-files images/* assets/*` 已无跟踪文件）；页脚版本徽章同步为 `网站版本 v  1.3.3`；`git status` 干净。用户自行部署线上。

## 访客计数器（不蒜子 busuanzi，未备份）
- 纯静态站无服务端，采用第三方嵌入方案「不蒜子」实现访问计数，Vercel 主站与 OSS 备用站均无需后端、自动生效。
- `index.html`：`</body>` 前引入 `<script async src="//busuanzi.ibruce.info/busuanzi/2.3/busuanzi.pure.mini.js"></script>`；页脚 `footer-row` 之后新增 `<p class="visitor-counter">`，含 `总访问 <span id="busuanzi_value_site_pv"> 次` 与 `独立访客 <span id="busuanzi_value_site_uv"> 人`（busuanzi 按 id 自动填充）。
- `css/style.css`：新增 `.visitor-counter` 暗灰小字、`.counter-sep` 暗红分隔点、`#busuanzi_value_site_pv/uv` 金色加粗数字，与页脚金色调一致。
- 注意：不蒜子按完整 URL（含域名）区分站点，Vercel 域名与 OSS 域名计数各自独立；本地 localhost 不计数，需部署到真实域名后才有数字。官方域名 ibruce.info 偶有波动，可换 jsdelivr 镜像 `//cdn.jsdelivr.net/npm/busuanzi@2.3.0`。
- 用户本地访问变慢诊断：根因为不蒜子脚本在本地也联网拉取（ibruce.info 不稳），已改为仅线上域名加载、本地跳过（判断 location.hostname 为 localhost/127.0.0.1/file:// 则不注入）。

## 模组概览卡片布局尝试（未备份）
- 用户要求"先试几个"：仅把主页【模组概览】6 卡片布局改为「图标+标题并排（标题在图标右侧居中）、介绍文字在下方整行」。
- 实现：容器 `cards cards-3` 加修饰类 `overview-cards`；`css/style.css` 用 `.overview-cards .card { display:grid; grid-template-areas:"icon title"/"desc desc" }` 限定作用域，不影响其他板块卡片。
- 状态：用户确认满意，已全局应用——CSS 规则去掉 `.overview-cards` 前缀、改为全局 `.card`（所有主页卡片统一生效），并移除 161 行 HTML 上的 `overview-cards` 限定类。全站仅 `index.html` 含 `.card`，其他详情页无卡片，故无需动其他页。未提交（待用户"备份"）。

## 卡片图标风格反复（未备份）
- 先按用户要求把全站 40 张卡片 emoji→暗金描边内联 SVG（26px）；用户最终改主意 revert 回 emoji，并放大 20%。
- 实现：从已提交 v1.3.3 取回 title→emoji 映射，脚本批量替换 40 张卡片，`grep` 确认 0 个 `<svg>` 残留；CSS `.card-icon` 调整为 `font-size:1.87rem`（1.7×1.1）、方块 54px，并删除 `.card-icon svg` 样式规则。全部改动未提交（待"备份"）。

## 装备详情「特等装备」→「基底装备」改名（未备份）
- 用户要求把第 05 板块卡片【特等装备】改显示名【基底装备】，并同步导航栏。
- 全站 ~40 个 HTML 页面批量替换 `特等装备`→`基底装备`（主页卡片 h3+aria-label、各页导航下拉链接、equipment-special.html 的 title/meta/nav-current/breadcrumb/h2 均改）；`grep` 确认 html 内 `特等装备` 清零。文件/链接仍用 `equipment-special.html` 不变。脚本 `_rename_special.py` 运行后已删除。
- 项目 MEMORY.md 编号约定同步：装备详情子项 3 由「特等装备」改为「基底装备」（备注原名与文件不变）。全部改动未提交（待"备份"）。

## 基底装备数据管道（筛选页前置，未备份）
- 用户把 D2 源文件放 `assets/mod/`：`armor.txt`（更新版，tab 分隔，227 条基底，含新增 M01-M09）、`objects.json`（D2 本地化表，358KB，每条含 `Key`+`zhCN`+`zhTW`；`Key` 即 armor.txt 的 `code` 列）。
- 临时脚本 `_build_base_csv.py`：用 `objects.json` 的 `zhCN` 作简中名、`zhTW` 作繁中名，生成 `assets/mod/armor_zh.csv`（列：code,name_en,name_zh,name_zh_tw,category,tier,defense_min,defense_max,defense_avg,durability,reqstr,qlvl,max_sockets）。校验 `hlm→头盔/頭盔` 通过；分类 type→中文(helm=头盔/tors=铠甲/shld=盾牌)，version→级别(0/1/2=普通/扩展/精英)。
- 待补：10 个 code（空 + M01-M09，Mod 新增基底）在 objects.json 无对应 Key，已标 `[?]`，需用户补中文名。
- 经验：D2 txt 用 DictReader 按列名取最稳（用户常增删列）；中文名优先取官方本地化表而非自翻，避免与用户译名不一致。后续需 weapons.txt/shields.txt 才能覆盖截图全部分类(武器/盾牌/手套/靴子/腰带)。

## 基底装备筛选页已做出（暗黑核风格，未备份）
- 用户确认 M01-M09 = 召唤物专用、不在游戏中，直接排除（新增 `_build_base_data.py` 过滤 `^M0[1-9]$` 与空 code）。
- `objects.json` 实际是 **list** 结构（非 dict），每条 `{Key,zhCN,zhTW,...}`；`Key`==armor.txt 的 `code`。中文名取自 `zhCN`，并 `clean_zh()` 剥掉 Mod 本地化前缀标记（如 `ㅱ¹ `）→ 干净中文名（便帽/头盔/棉布甲…）。
- `armor.txt` 实际覆盖全护甲子类：type 含 helm/tors/shld/glov/boot/belt/pelt/phlm/ashd/head/grim/circ。按 TYPE_GROUP 归并为 6 大类（头盔/铠甲/盾牌/手套/靴子/腰带）以贴合截图分类。`version` 出现 0(普通,92) 与 **100(125)**——100 为 Mod 自定义编码，暂映射为「扩展」，**需向用户确认 100 含义**（精英？）。
- 产出：`js/base-items.js`(window.BASE_ITEMS, 217 条) + `js/base-db.js`(搜索/分类/级别/排序/译名对照，纯前端) + `equipment-special.html` 占位改为筛选 UI（搜索框+分类/级别下拉+排序+统计条+卡片网格）+ `css/style.css` 新增 `.base-db` 系列样式。`_build_base_data.py` 保留（可重跑）。全部未提交（待"备份"）。
- 注：equipment-special.html 仍是旧版 footer（觉醒之夜/旧链接/当前版本 v1.1.0），本次未动；如需与其他页一致可后续统一。

## 基底装备 tier 修正：改用 objects.json 上标 ¹²³（未备份）
- 用户明确：普通/扩展/精英 判定依据为 `objects.json` 中文名里的上标——`¹`=普通、`²`=扩展、`³`=精英（名字形如 `ㅱ¹ 便帽`/`ㅱ² 战帽`/`ㅱ³ 军帽`）。**之前误用 armor.txt 的 `version` 列（0/100 仅 classic/expansion 概念）是错的**，已纠正。
- 改 `_build_base_data.py`：新增 `SUP_MARK = {"\u00b9":"普通","\u00b2":"扩展","\u00b3":"精英"}` 与 `tier_from_zh()`（用 Unicode 转义避免文件编码歧义）；`load_objects()` 现返回 `{zh,tw,tier}`；主循环 tier 优先取自 objects.json 上标，`version` 仅作缺失回退。脚本注释同步。
- 重跑脚本：217 条全部正确判定（0 回退/0 缺中文名），分布 **普通 73 / 扩展 72 / 精英 72**（抽查 cap 便帽→普通、xap 战帽→扩展、uap 军帽→精英，均对）。`armor_zh.csv` 与 `js/base-items.js` 同步更新。
- 前端：`css/style.css` 补 `.base-tier.tier-精英`（紫 `#d39bff`）；`js/base-db.js` 的 tier 下拉按 `TIER_ORDER=["普通","扩展","精英"]` 固定排序；`equipment-special.html` meta 描述旧词「特等品质」→「基底品质」。tier 下拉由 JS 从 DATA 动态生成，加精英自动出现。
- 项目 MEMORY.md 新增「基底装备数据生成约定」小节（数据来源/脚本/ tier 必须取 ¹²³ 不用 version / 排除 M01-M09 / 配色类）。全部未提交（待"备份"）。

## 已备份 v1.3.4（2026-08-14 23:4x）
- 本次"备份"把 v1.3.3 之后的全部未提交改动一并提交：commit `398cc81`，46 文件（4072 插入/67 删除），新增 `_build_base_csv.py`/`_build_base_data.py`/`js/base-db.js`/`js/base-items.js`。
- 内容涵盖：不蒜子访客计数器；全站卡片图标并排+emoji放大10%；物品和配方编号 8→7；特等装备→基底装备改名及全站导航同步；基底装备筛选页（equipment-special.html + base-items.js + base-db.js + .base-db 样式），tier 修正为按 objects.json 上标 ¹²³（普通73/扩展72/精英72），名称剔除装饰符号 ★。
- 按约定备份前已将 `index.html` 页脚 `.version-badge` 同步为 `网站版本 v  1.3.4`，与 git tag 一致。
- 标签：`git tag v1.3.4`。当前 latest = v1.3.4（v1.0.9→v1.1.0→v1.1.1→v1.2.0→v1.3.0→v1.3.1→v1.3.2→v1.3.3→v1.3.4）。本地仓库，无 remote。
- 注：equipment-special.html 仍为旧版 footer（觉醒之夜/旧链接/当前版本 v1.1.0），与主页 footer 风格未统一，待用户要求时再处理；其余全站页面 footer 已为统一版。

## 基底装备名称剔除装饰符号 ★（未备份）
- 用户要求：极个别装备名含游戏显示符号 `★`（如 `法师铠甲★★`、`头环★`），非真实名称，列表与网页显示都要去掉。
- 扫一遍确认护甲 code 在 objects.json 中除 `★`(U+2605) 外，其余非常规字符都是 Mod 前缀 `ㅱ` 系列与 tier 上标 `¹²³`（已由 `clean_zh` 前缀剥离），故只需专门剔除 `★`。
- `_build_base_data.py`：`clean_zh()` 末尾新增 `SYMBOL_STRIP` 集合（★ 为主，附带 ☆◆●◇■□▲▼ 等常见装饰符）全局剔除后两端去空格。重跑后 CSV 与 `js/base-items.js` 残留 `★` 均为 0（抽查 xtp 法师铠甲/ci0 头环/uhm 巨角头盔/uit 君主盾 均干净）。网页读 `base-items.js`，自动同步无 ★。全部未提交（待"备份"）。

## 全站页脚统一为 v1.3.4 主页样式（未备份）
- 用户要求把详情页页脚统一成主页样子。核查发现不止 equipment-special.html，全站 39 个详情页仍是旧版 footer（footer-tag「觉醒之夜已经降临」+ 站内 4 锚点链接 + 「© 保留所有权利」+「当前版本 v1.x.x」），统一替换为 index.html 的新版 footer：王+新王觉醒 brand-line、不蒜子 visitor-counter、5 外链（赞助链接/新王觉醒/魔王降临/王者归来/Mod小站）的「资料站链接」、By 隔壁大王、网站版本 v1.3.4 徽章。
- 用临时脚本批量替换（正则匹配 <footer class="site-footer">…</footer>），并给 39 个缺计数器的详情页补入不蒜子 busuanzi 脚本（location.hostname 判断，localhost/127.0.0.1/file:// 跳过注入）。
- 校验：全站 41 个 html 旧 footer-tag 残留 0；By 隔壁大王 / 版本徽章 v1.3.4 / busuanzi 脚本各 41 处；抽查 author.html 末尾结构正确。临时脚本运行后已删除。
- 全部未提交（待“备份”）。当前最新 tag 仍为 v1.3.4，全站页脚徽章已一致，无需改版本号。
- 注：v1.3.4 备份日志中「其余全站页面 footer 已为统一版」一句不准确——当时实际所有详情页都还是旧 footer，仅 index.html 是新版；本次才真正全站统一。
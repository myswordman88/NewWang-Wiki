# 2026-08-13 工作日志

## 新王觉醒官网
- 修复导航下拉菜单交互：原 `.dropdown` 用 `top: calc(100% + 6px)` 导致与导航项之间有 6px 物理间隙，鼠标横移经过时丢失 hover 焦点、菜单收起。改为 `top: 100%`（紧贴导航项底部），并删除原用于填视觉缝的 `.dropdown::before` 三角。现下拉与导航栏无缝衔接，hover 不再误关闭。（css/style.css）
- 标题渐变配色调整：`.hero-title .accent`（「觉醒」二字）由金红渐变改为白→红渐变 `linear-gradient(135deg, #ffffff 0%, #ff7a6a 42%, var(--accent) 100%)`，白色仅起点少量、红为主体且红端明亮不暗（暗黑经典白红）。「新王」二字保持白字。（css/style.css）
- 标题统一渐变：改为整段「新王觉醒」作为整体从左到右渐变。`.hero-title` 加 `background: linear-gradient(90deg, #ffffff 0%, #ff7a6a 42%, var(--accent) 100%)` + `background-clip:text; color:transparent`；`.hero-title .accent` 去掉自身独立渐变、仅 `color:transparent` 继承父级渐变，使四字连贯无断点。（css/style.css）
- 标题整体提亮：用户反馈原红端（--accent 深余烬红）偏暗，改为白→浅红更明亮的渐变 `linear-gradient(90deg, #ffffff 0%, #ffc9bd 50%, #ff9384 100%)`（css/style.css）
- 标题配色回退为最初版：`.hero-title` 改回纯白 `#ffffff`（去掉整段白红渐变）；`.hero-title .accent`（「觉醒」二字）恢复金色渐变 `linear-gradient(135deg, var(--gold-soft), var(--accent-2))`（浅金→古铜金）。即「新王」白、「觉醒」金。（css/style.css）
- 卡片样式调整：`.card` 移除右上缺角的 `clip-path`（保留 `border-radius` 圆角矩形），并加 `overflow: hidden` 让顶部金条贴合圆角；`.card::after` 由「仅左下角单 L 形 HUD」改为「四角括号」（用 8 层 `linear-gradient` background 在 inset:10px 的伪元素上画出四个角的 16px×2px 括号，且 `border-radius:0` 避免伪元素自身圆角裁掉括号尖角），悬停时四角同时出现古铜金描边。（css/style.css）
- 卡片悬停动画增强：`.card` 过渡加入 `box-shadow`（0.3s）；`.card:hover` 上浮由 -6px 加大到 -10px、加古铜金辉光 `box-shadow`、边框转 `--accent-2`、顶部金条 `::before` opacity 0.75→1，动态感更强。`@media (prefers-reduced-motion: reduce)` 已全局关闭过渡/动画，无障碍兼顾。（css/style.css）
- 修复卡片悬停上浮完全无效：根因是 CSS 优先级冲突——`.reveal.is-visible { transform: none; }`（第390行，优先级0,2,0）与 `.card:hover { transform: translateY(-10px); }`（第326行，同为0,2,0）相等，但 `.reveal.is-visible` 在文件中更靠后而胜出，把 hover 的 transform 覆盖成 none，导致卡片悬停纹丝不动。改为更高优先级选择器 `.cards .card:hover`（0,3,0）后悬停上浮生效。（css/style.css）
- 已启用 Git 版本控制：`git init`（分支重命名 main），首个提交 `e0a5855`(v1) 锁定暗黑奇幻基线；本日追加注解标签 **`v1.0.1`**（指向 e0a5855），用于标记该稳定版本。回退可用 `git checkout v1.0.1 -- <file>` 或 `git revert`。（本地仓库，未推远程）

## 版本控制
- 本工作区已启用 Git 版本管理：`git init` 于 `C:/Users/wyj70/WorkBuddy/2026-08-11-22-00-10`，默认分支已重命名为 `main`。首个提交 `e0a5855`（tag 语义 v1）锁定"暗黑奇幻风格基线"满意版本，包含 index.html / css/style.css / js/main.js / DEPLOYMENT.md（`.workbuddy/` 已写入 .gitignore 排除）。git 本地身份设为 WorkBuddy / user@workbuddy.local。后续每次大改建议 `git add -A && git commit -m "..."` 留档，回退用 `git log` + `git checkout <版本> -- <文件>`。
- **项目已迁移至新硬盘**：用户将整文件夹（含隐藏 `.git`）复制到 `F:\Work_Test\WebSite\2026-08-11-22-00-10`，已验证 `.git`、标签 `v1.0.1`、提交 `e0a5855` 均完整保留，工作区 clean。后续工作以新路径为准（原 `C:/Users/wyj70/WorkBuddy/2026-08-11-22-00-10` 仍保留，确认新位置无误后可删）。新路径本地预览已重新拉起（8100，python http.server）。
- **新增详情页 feature.html（特色简介）**：创建 `F:/Work_Test/WebSite/2026-08-11-22-00-10/feature.html`，复用首页同一套 header/导航/footer 与暗黑奇幻样式（顶部导航"不变"），中间内容区显示面包屑 + 标题"特色简介" + 内容"【特色简介】：测试一下几个字即可" + 返回首页按钮。首页导航下拉"模组概览 → 特色简介"链接由 `#overview-feature` 改为 `feature.html` 实现跳转；其余导航项的锚点链接在详情页改为 `index.html#xxx` 以保可用。css/style.css 新增 `.breadcrumb / .detail-panel / .detail-text / .detail-back` 样式。预览验证 feature.html、css 均 200。
- **首页特色简介卡片可点击**：将 index.html 中 `id="overview-feature"` 的 `<article class="card">` 改为 `<a class="card card-link reveal" href="feature.html">`，鼠标悬停变手型光标（`<a>` 默认 + 显式 `.card-link { cursor: pointer; }`），点击整卡跳转到 feature.html 详情页。HUD 四角 `::after` 已设 `pointer-events: none` 不挡点击。
- **模组概览 5 卡片全部可点击化**：将首页「模组概览」板块其余 4 张卡片（作者简介/赞助详情/下载地址/Mod 讨论社群）由 `<article class="card reveal">` 改为 `<a class="card card-link reveal" href="...">`，与特色简介卡片同样适配（手型光标 + 整卡点击跳转）。为此新建 4 个详情页 `author.html / donate.html / download.html / community.html`（沿用 feature.html 模板：同一导航/footer、面包屑、标题、占位文字「【XX】：测试一下几个字即可」、返回首页按钮）。首页与 feature.html 的「模组概览」下拉导航 4 项同步改为指向对应详情页（更新日志仍指向 `#changelog` 子版块锚点）。
- **【更新日志】简化为单卡片并归位**：删掉首页「模组概览」下原 `#changelog` 子版块（含历史改动/完整规划/制作进度 3 张卡片）。改在「下载地址」卡片之后插入一张可点击的【更新日志】卡片 `<a class="card card-link reveal" id="overview-changelog" href="changelog.html">`，现模组概览网格顺序为：特色简介→作者简介→赞助详情→下载地址→更新日志→Mod讨论社群（6 张卡片，cards-3 两行）。新建 `changelog.html` 详情页（同模板、kicker「模组概览 · 06」、占位文字「【更新日志】：测试一下几个字即可」）。同步把首页导航下拉、首页页脚、以及 5 个详情页（feature/author/donate/download/community）中原本指向失效锚点 `#changelog` 的链接全部改为 `changelog.html`，全站已无 `#changelog` 锚点残留。
- **Git 提交并打 `v1.0.3` 标签**：将详情页体系（feature/author/donate/download/community/changelog 5 个页面）+ 首页 6 卡片可点击化 + 更新日志归位 + css/style.css 配套样式，全部 `git add -A` 提交（提交 `712ab30`，message「v1.0.3: ...」），并 `git tag -a v1.0.3`（附注标签，指向 712ab30）。仓库现标签链 `v1.0.1 → v1.0.2 → v1.0.3`，工作区 clean。注：git 报告 LF→CRLF 行尾归一化警告（Windows 环境），不影响内容与版本。
- **图片体积优化（WebP 无损）**：用户本机无专门压缩 skill，我用隔离 Python 环境（managed venv）装好 Pillow 12.3.0（WebP/AVIF 均支持）。项目根目录有 4 张「特色简介」相关 PNG 截图（共享仓库/魔盒/通货仓库，合计 2.7MB）。编写 `tools/optimize_images.py`（支持 lossless/webp/avif/keep 四种模式 + 体积对比报告），运行 `python tools/optimize_images.py . --out assets --mode lossless` 转成 WebP 无损存到 `assets/`，合计 1.7MB、省 36.3%，并用 ImageChops 像素级校验与原图「完全一致（无损）」。原 PNG 仍留在根目录（待用户确认后可删或移入 source/）。复用命令：`C:/Users/wyj70/.workbuddy/binaries/python/envs/default/Scripts/python.exe tools/optimize_images.py <源目录> --out assets --mode lossless`。
- **图片进一步有损压到 ~100KB/张**：用户接受牺牲少量清晰度。新建 `tools/optimize_to_target.py`（有损 WebP + 必要时按比例缩分辨率，自动逼近目标体积，默认 100KB）。运行 `python tools/optimize_to_target.py . --out assets --target 100`，4 张图压到 80.9/82.4/85.3/91.1 KB（质量 85/90/75/75，scale=1.00 无需缩小，因原图最宽仅 854px）。此版覆盖了 assets/ 里之前的 WebP 无损文件；根目录原 PNG 仍为备份。
- **重写特色简介详情页**：按用户提供的文字与 5 张 R2 图床外链，设计并写入 `feature.html`。新增 `css/style.css` 中 `.feature-*` 系列样式（主视觉图、章节标题、特性列表、统计标签、2 列图片画廊、说明条）。页面结构：面包屑 + 标题区 + 主视觉图 + 1.1.1 Mod 简介 + 1.1.2 Mod 特色（大箱子/大背包/大魔盒 + 数据标签 + 4 张游戏截图画廊 + 手柄适配提示）+ 返回首页。图片全用 R2 外链以减小仓库体积，并加 `loading="lazy"` / `decoding="async"`。
- **提交更新**：将 feature.html 重写、style.css 样式、图片压缩脚本 tools/、assets/ 压缩图、根目录原 PNG 一起 `git add -A` 提交（`48a5771`）。注意本次提交也带入了 `index.html` 的细微文案改动（模组概览 lead 文字更新）。当前未打新标签，保留在 v1.0.3 之后的提交上。
- **特色简介排版微调（按用户反馈）**：保留暗黑奇幻风格不变，只改两处——① 数字标签 `.feature-stat` 由「红底白字渐变」改为「淡绿文字 `#8fe0a8`」（去 background/padding/text-shadow）；② 图片由底部 2 列画廊（`.feature-grid`, width:100% 会放大）改为「文字介绍 + 配图」逐块堆叠（新增 `.feature-block`），每张图按原始尺寸不缩放（`img { width:auto; max-width:100% }`）。重排为 5 块：大背包(15×11,图1) / 个人仓库(16×16,图2) / 共享仓库(16×16,图4) / 赫拉迪姆魔盒(10×10,图3) / 通货页面(10×10,图5)，顶部独立主视觉图已移除（图1 移到大背包块下）。图片按文件名内容对应配对：共享仓库用 `特色简介_4_共享仓库截图.jpg`（即用户说的「第4张」，其内容本就是共享仓库截图，故未用「第3张」魔盒截图）。提交 `0c37653`。
- **修复 feature-stat 数字显示成橙色**：`<strong class="feature-stat">` 被 `.feature-block-text strong`（古铜金 `--accent-2`，暗底偏橙）优先级盖掉，数字显示成橙色而非淡绿。修复：把规则改为 `.feature-stat, .feature-block-text strong.feature-stat`（提高优先级）确保淡绿 `#8fe0a8` 生效，并将字号由约正文大小提到 `1.18rem`。提交 `a032b1d`。
- **Git 打 `v1.0.4` 标签**：在 `a032b1d`（feature-stat 数字颜色修复）上 `git tag -a v1.0.4`（附注标签）。至此【特色简介】详情页工作收尾，标签链 `v1..0.1 → v1.0.2 → v1.0.3 → v1.0.4`，工作区 clean。注：feature.html 实际内容仍用 R2 外链图片（5 张），图片不占仓库体积。
- **修复 feature-stat 颜色不生效 + 放大字号**：用户实测数字仍显示古铜金（被误认橙色）。根因：数字写成 `<strong class="feature-stat">` 位于 `.feature-block-text` 内，而 `.feature-block-text strong { color: var(--accent-2); }` 选择器优先级(0,2,1)高于 `.feature-stat`(0,1,0)，盖掉了淡绿。修复：将 `.feature-stat` 规则扩展为 `.feature-stat, .feature-block-text strong.feature-stat`（优先(0,3,1) 胜出），并加 `font-size: 1.18rem` 略放大。提交 `a032b1d`。
- **作者简介详情页完成**：`author.html` 重写，按截图内容——1.2 作者简介 / 1.2.1 作者介绍（金句标语卡「白天在三尺讲台前传道授业…」+ 数学教师/2021.11 接触 D2R Mod/经历四大型版、几十小版本/3 款 Mod 列表(王者归来·魔王降临·新王觉醒) / B站Up主「隔壁大王讲数学」/ 致谢与捐助引导）。新增 `.author-tagline/.author-mod/.author-cta` 样式，页脚版本徽章 v1.0.4。提交 `2eee6ea`。
- **赞助详情页完成 + 样式微调**：`donate.html` 重写（1.3 赞助渠道总览3项 + 1.3.1 VIP群188元 + 1.3.2 冠名赞助装备含价格表）。按反馈：状态徽章 `.status-badge` 改黑底荧光绿(已开放)/黑底红描边无填充(未开放)、标题内徽章同步、编号 `::marker` 改淡蓝 `#8ec5ff`；后又因徽章继承父级行高(列表 2.2 vs 标题 1.9)导致胖扁不一，加 `line-height:1` 修复。提交 `3b05332`、`7ca6517`。
- **下载地址详情页完成**：`download.html` 重写——1.4.1 游戏本体(3.0 Ver 3.0.91735, 夸克+码44xv) / 1.4.2 Mod文件(新王觉醒Mod, 码5RTx) / 1.4.3 全地图插件(PrimeMH, 码Xhpw, 红色在线警告) / 1.4.4 伤害显示(D2R Damage Numbers, 码TXB2, 红色警告) / 1.4.5 最新启动器(D2RLoader, 码wW7t, 红色警告) / 1.4.6 其他(2.7/2.4客户端、魔王降临已停更、王者归来已完结)。新增 `.download-card/.download-icon/.download-warning/.download-link/.download-code/.download-label` 样式，页脚 v1.0.4。提交 `a2dc770`。
- **更新日志页改为 wolai 外链**：用户暂不做更新日志详情内容，`changelog.html` 占位面板改为跳 `https://www.wolai.com/gWD5fJwKBybSkXHjnNuDcs` 的外链卡片（target=_blank, rel=noopener），新增 `.changelog-link` 古铜金边框悬停辉光样式，lead 文案同步，页脚版本徽章由 v1.0.2 修正为 v1.0.4。提交 `b2717a7`。
- **讨论社群详情页完成 + 全站重命名**：`community.html` 重写——1.5 讨论社群 / 引导语 / 1.5.1 讨论 QQ 群（1群273598961 / 2群1016534254 / 3群649515626）/ 群规说明（2000人上限、定期清理潜水、禁止讨论 Mod 修改）/ QQ 频道 909sydu494 / B站入口按钮。新增 `.community-grid/.community-card/.community-number/.community-channel` 样式。为保持首页卡片四字整齐，全站「Mod 讨论社群」统一改为「讨论社群」（index.html 卡片标题/aria-label、导航下拉；feature/author/donate/download/changelog 导航下拉同步）。提交 `0b197bf`。
- **讨论社群页多轮微调**：① 主标题去「1.5」编号、B站按钮移至页底；② 面包屑 `.breadcrumb` 字号 0.8→1rem 对齐导航栏；③ 子标题 1.5.1 改 `.feature-section-title` 与其他页一致；④ QQ群卡片由 `<a>` 改 `<div>` 不可点击(避免打不开网页)但保留悬停上浮、加 `cursor:default`；⑤ 页底引导框 `.detail-panel` 收窄居中(max-width:680px; margin:auto)、「前往B站主页」按钮补 `btn` 基类放大(padding 1rem 2.1rem, font 1.08rem)且间距 1.2→1.8rem；⑥ 群标签「1/2/3群」`.community-label` 字号 0.8→1.4rem、颜色改浅蓝 `#8ec5ff`。提交 `87933b2 / 4c6c0e6 / 27b5956 / 828dca2`。
- **主页 Hero 区整体放大约20%**：eyebrow 1.02→1.22rem、标题 clamp(3.6rem,13.2vw,7.68rem)、副标题 clamp(1.2rem,2.88vw,1.42rem)、倒计时标签 1.00→1.20rem、方框 min-width 76→91px/padding 1.08rem 0.72rem、数字 clamp(2.28rem,7.2vw,3.36rem)、天时分秒 0.72→0.86rem、分隔符放大、版本卡 dt 0.95→1.14rem/dd 1.3rem/padding 放大。提交 `7d62771`；后又单独把天时分秒 0.86→1.03rem(再+20%) 提交 `306e8fe`。
- **主页标题四字错开弹跳动画**：`index.html` 标题 `新王<span class="accent">觉醒</span>` 拆为四个 `<span class="hero-char">`(保留「觉醒」金渐变)。CSS 加 `.hero-char{display:inline-block; animation:hero-jump 10s linear infinite}` + nth-child delay 0/0.3/0.6/0.9s 依次跳；`@keyframes hero-jump` 设计每10秒仅极短窗口(初始约7.4%)做「起跳→顶(-26px)→落地→轻微回弹(-6px)→静止」，含 `prefers-reduced-motion` 降级。后按反馈把弹跳窗口压缩到约5%(速度再快约50%)。提交 `362c962 / 6dc8eea / e6b9441`。
- **Git 打 `v1.0.5` 备份标签**：在 `e6b9441`（标题弹跳再快50%）上 `git tag -a v1.0.5`（附注标签），覆盖「模组概览全 6 详情页完成 + 主页 Hero 放大约20% + 标题四字错开弹跳动画」这一整批改动。标签链 `v1.0.1 → v1.0.2 → v1.0.3 → v1.0.4 → v1.0.5`，工作区 clean。仓库仍仅本地（无 remote）。
- **第二部分·新手专区卡片可点击化**：仿模组概览做法，`index.html`「新手专区」板块三卡片（安装指南/常见问题/新手入门，原 `<article class="card reveal" id="newbie-*"`，第193/198/203行）改为 `<a class="card card-link reveal" id="newbie-*" href="guide.html|faq.html|beginner.html" aria-label>`，手型光标 + 悬停上浮（`.card-link`/`.cards .card:hover` 已有，自动生效）。新建 3 个详情页 `guide.html / faq.html / beginner.html`（沿用 feature.html 模板：同导航/footer、面包屑「首页/新手专区/XX」、kicker「新手专区 · 01/02/03」、占位文字「【XX】：测试一下几个字即可」、返回首页按钮，footer 版本 v1.0.4）。全站 9 个页面（index + 8 详情页）「新手专区」下拉三项锚点（`#newbie-install/#newbie-faq/#newbie-start`）用 sed 整行替换为 `guide.html/faq.html/beginner.html`，无失效锚点残留。提交 `0d3e2d8`。
- **安装指南详情页完成**：`guide.html` 由占位页重写为正式内容「2.1.1 学习版客户端」，按截图整理成三步：① 安装游戏本体（下载/解压/首次进游戏确认版本）；② 安装 MOD（版本匹配、目录路径、新建 Mods 文件夹、确认 `E:\Diablo II Resurrected\mods\NewWang\NewWang.mpq` 目录结构）；③ 启动 MOD（建快捷方式、目标栏追加 `-mod NewWang` / `-enablerespec`、检查完整命令行、运行确认）。新增 `.guide-video-card/.guide-label-*/.guide-link/.guide-path/.guide-code/.guide-param*/.guide-command/.guide-list` 等样式（css/style.css），列表编号/项目符号统一淡蓝 `#8ec5ff`，命令行参数分别用浅蓝/淡橙突出，路径用金底标签，下载链接指向站内 `download.html`。页脚版本徽章同步为 v1.0.5。
- **常见问题详情页完成**：`faq.html` 由占位页重写，按截图整理「1. 闪退问题」及 4 类排查：① 版本不匹配；② 存档不兼容（含存档路径 `C:\Users\...\mods\NewWang`）；③ 错误的命令行参数（强调不要加 `[-txt]`）；④ 其他报错情况。新增 `.faq-intro`（紫边提示条）、`.faq-cause`（浅蓝）、`.faq-fix`（淡红）、`.faq-path`（绿色路径标签）、`.faq-param`（淡橙参数）样式（css/style.css）。页脚版本徽章同步为 v1.0.5。
- **新手入门占位页更新**：`beginner.html` 占位文字改为「【新手入门】板块尚在建设中，内容会逐步补充完善，敬请期待。」，表达留坑后续完善之意。页脚版本徽章同步为 v1.0.5。
- **Git 打 `v1.0.6` 备份标签**：在 `262484e`（新手专区详情页内容完成）上 `git tag v1.0.6`。覆盖「安装指南正式三步流程 + 常见问题闪退四类排查 + 新手入门占位 + FAQ/Guide 专属样式 + 页脚版本徽章同步 v1.0.5」。标签链 `v1.0.1 → v1.0.6`，工作区 clean。仓库仍仅本地（无 remote）。
- **导航栏【模组特色】改名为【全新系统】**：全站 10 个页面（index + 9 详情页）下拉触发项 `>模组特色</a>` 用 sed 批量改为 `>全新系统</a>`（仅匹配 `</a>`，不动 `<!-- 3. 模组特色 -->` 注释）。下拉子项与锚点 `#changes-*` 不变。
- **下拉菜单「缝隙丢焦点」修复**：`.dropdown` 初始 `translateY(6px)` 使触发项与面板间有 6px 死区，鼠标经过丢失 `:hover` 收起面板。在 `css/style.css` 给 `.dropdown::before` 加透明桥接区 `top:-12px;height:12px`，继承 `pointer-events`，展开时自动可悬停，填补死区。移动端 `.dropdown::before{display:none}` 不受影响。
- **全新系统 4 卡片可点击化 + 详情页**：`index.html`「全新系统」板块 4 卡片（基础改动一览/觉醒系统/转职系统/合成配方，原 `<article class="card reveal" id="changes-*">`，第221/226/231/236行）改为 `<a class="card card-link reveal" id="changes-*" href="changes-basic.html|changes-awaken.html|changes-class.html|changes-recipe.html" aria-label>`，悬停上浮 + 手型光标（`.card-link`/`.cards .card:hover` 已有）。新建 4 个详情页 `changes-basic.html / changes-awaken.html / changes-class.html / changes-recipe.html`（沿用 beginner.html 模板：同导航/footer、面包屑「首页/全新系统/XX」、kicker「全新系统 · 01~04」、占位文字「【XX】板块尚在建设中，内容会逐步补充完善，敬请期待。」、返回首页按钮，footer 版本 v1.0.5）。全站 10 页面「全新系统」下拉 4 锚点（`#changes-basic/-awaken/-class/-recipe`）用 sed 整行替换为对应 .html，无失效锚点残留（共 44 处新链接）。
- **PDF 读取并转网页（基础改动一览）**：用户把 `assets/4.1 基础改动一览.pdf`（17MB，16 页，图文混排）放入 assets。Read 工具无法直接读（二进制），改用 pypdf（隔离 venv `envs/default`，Scripts/python.exe）提取文字（5602 字，结构 4.1.1 功能性→4.1.4 数值改动），并用 pypdf 抽取 29 张内嵌图（14.6MB 原始），Pillow 压缩为 WebP（约 100KB/张，共 1.66MB）存 `assets/pdf_images/pX_Y.webp`。`changes-basic.html` 由占位页重写为正式内容，沿用 feature.html 版式（`.feature-section`/`.feature-block`/`.feature-figure`/`.feature-stat`/`.feature-note`），按 4.1.1~4.1.4 分节，29 张图按页嵌入对应小节，关底 Boss 复活表用新增 `.feature-table` 样式（css/style.css，同 price-table 暗色风）。清理原始 PNG 与文本 dump，保留 WebP + 源 PDF。注：PyMuPDF 因网络只下小包装不上，pypdf 即可满足文字+图片抽取。
- **图片逐步迁移到 R2 图床（基础改动一览）**：用户把 `assets/pdf_images/` 的图陆续上传到 R2（`https://pub-2c9e779ff6804644883f17859b0108be.r2.dev/%E5%9F%BA%E7%A1%80%E6%94%B9%E5%8A%A8/pX_Y.webp`，目录 `基础改动` URL 编码）。本次先用 Python 脚本把 14 张（`p1_0/1_1/1_2/p2_0/2_1/p3_0/3_1/p4_0~4_3/p5_0/p10_0/10_1`）从 `assets/pdf_images/pX_Y.webp` 替换为图床链接，保留 `loading="lazy"` 等属性。剩余 15 张仍本地：`p5_1~5_4/p6_0/6_1/p8_0/8_1/p9_0~9_6`，待用户后续给链接再补。约定：用户分批给文件名→URL，逐批替换，保持外链降流量。
- **图床迁移完成 + 删本地图**：用户补全剩余 15 张链接，Python 脚本替换后 `changes-basic.html` 共 29 张图全部为 R2 图床链接、本地零残留（`grep -c assets/pdf_images` = 0）。按之前约定删除 `assets/pdf_images/` 整个文件夹（`rm -rf`，沙箱已授权），源 PDF `assets/4.1 基础改动一览.pdf` 保留。仓库体积进一步下降。改动未提交。
- **基础改动一览图片改为并排网格**：用户在 `changes-basic.html` 要求把「大箱子+大背包+大魔盒」起始的连续 26 张图（文档顺序 p1_0→p9_5）按规则分组并排：1,2/3,4/5单/6,7/8单/9-11(3)/12-14(3)/15,16/17,18/19,20/21,22/23-26(4)，p9_6 与 p10_0/p10_1 保持单列。在 `css/style.css` 新增 `.fig-row`（flex 并排、gap、4 类 `.two/.three/.four/.single`，`.single` 居中限宽 640px，≤680px 移动端纵向堆叠、`.feature-figure` flex:1 1 0 等宽）。用 Python 脚本按全局 figure 序号倒序把连续图包进 `.fig-row`（共 12 行：7×two+2×three+1×four+2×single），29 张图总数不变。改动未提交。
- **全站去掉「基础改动一览」的「一览」二字**：用户要求把页面与导航栏中「基础改动一览」统一改为「基础改动」。用 sed 在全站 11 个 .html 文件中全局替换，覆盖：导航栏下拉项（index.html 第63行）、首页卡片标题与 aria-label（index.html 223/221 行）、各详情页面包屑下拉（10 页第52 行）、`changes-basic.html` 的 title/meta description/h2 大标题/当前面包屑（第6/7/108/103 行）。grep 验证无残留。改动未提交。
- **Git 打 `v1.0.7` 备份标签**：在 `bf04ddd`（v1.0.6 后累积更新）上 `git tag v1.0.7`。覆盖：① 导航栏「模组特色」→「全新系统」全站改名；② 下拉缝隙丢焦点修复（`.dropdown::before` 桥接区）；③ 全新系统 4 卡片可点击化 + 4 个详情页（changes-awaken/basic/class/recipe.html）；④ 基础改动页 PDF 转网页（29 图）、图床迁移、并排网格、去「一览」；⑤ 页脚版本徽章 v1.0.5。共 15 文件 +989 行。源 PDF `assets/4.1 基础改动一览.pdf`（17MB）刻意不纳入 git（页面已全用图床外链，进库仅膨胀仓库），仅留磁盘。标签链 `v1.0.1 → v1.0.7`，工作区干净（仅 PDF 未跟踪）。仍仅本地无 remote。
- **觉醒系统详情页完成（changes-awaken.html）**：用户提供导出 HTML `assets/导出网页/4.2 觉醒系统.html`（252KB 单行，含 8 张 `media/image*.png` 截图）。用 Python 正则解析 HTML 提取结构化文字（标题/段落/列表/表格/图片标记），整理为 4.2.1 简介（王之馈赠/魔盒合成/奈非天法典+1点）+ 4.2.2 六职业觉醒（冰冷/闪电/火焰=法师，枪矛/生存/弓弩=亚马逊）。8 张 PNG 用 Pillow 压缩为 WebP（13-54KB/张，共 356KB）存 `assets/awaken_images/`。`changes-awaken.html` 由占位页重写为正式内容，沿用 feature.html 版式（`.feature-section`/`.feature-subsection`/`.feature-subsection-title`/`.feature-block`/`.feature-list`/`.feature-stat` 数值高亮/`.feature-note`/`.feature-figure`/`.feature-table`），每职业含 7 阶觉醒方法（ol）+ 觉醒效果（ul，百分比数值 `<strong class="feature-stat">` 高亮）+ 对应截图；生存系额外含「幻影分身」召唤物属性表（13 行，`.feature-table`）。新增 `.feature-subsection-title .muted`、`.feature-subsection` 分隔样式（css/style.css）。`assets/导出网页/` 源 HTML 保留作参考，临时 `_awaken_text.txt` 已清理。改动未提交。
- **【Bug 修复】超长 `.reveal` 区块在视口外永不显示**：用户反馈 `changes-awaken.html` 只能看到 4.2.1 的奈非天法典图，4.2.2 六职业详情整块空白。排查：HTML 标签全平衡（article/div/li 等均开闭相等），reveal 机制正常；根因是 `js/main.js` 滚动揭示用的 `IntersectionObserver` 原阈值 `threshold: 0.12`——要求元素 12% 进入视口才触发。4.2.2 是超长区块（≈5000-9000px），在普通视口里最高可见比例≈10% 永远达不到 12%，`isIntersecting` 始终 false，元素保持 `opacity:0` 不可见；短区块（4.2.1）则正常。修复：把该 observer 阈值改为 `0`（`{ threshold: 0, rootMargin: "0px 0px -8% 0px" }`），元素任意像素进入视口即触发，超长内容也能正常淡入。这是共享脚本，改完对全站所有长 `.reveal` 区块都更健壮。**教训（可复用）**：用 IntersectionObserver 做 reveal 时，不要给可能高于视口的元素设 `threshold > 0`，否则长内容永不显示；用 `threshold: 0` 或 `threshold: [0, 0.1]` 才安全。改动未提交。
- **觉醒系统图片迁移图床 + 删 5 张**：用户提供 R2 链接（`https://pub-2c9e779ff6804644883f17859b0108be.r2.dev/%E8%A7%89%E9%86%92%E7%B3%BB%E7%BB%9F/` =「觉醒系统」编码）。页面实际只有 `image_1~7.webp`（7 张），无无序号的 `image.webp`（用户给的 `觉醒系统/image.webp` 链接在页面无对应引用——参考 HTML 从未引用 media/image.png，已说明忽略）。脚本替换 `image_1`、`image_2` 为图床链接（共 2 处），并用正则删除 `image_3~7` 整段 `<figure>`（5 个）。页面本地图引用归零（2 张 R2 链接），本地 `assets/awaken_images/` 目录整体删除（沙箱授权 `rm -rf`），与基础改动页保持一致；源参考 HTML 与 media 仍在 `assets/导出网页/`。改动未提交。
- **基础改动并排图框比例修正 + 全局 fig-row 对齐**：① 先用「突破 1200px 内容栏、加宽到 1500px」放大三/四列图，用户反馈「不好看、不要让它们突破 1200」。② 实测图片真实比例：三列 6 张（p4_1~p5_2）≈434×750（≈4:7，竖图），四列 4 张（p9_2~p9_5）≈330×420（≈4:5，竖图）。撤回加宽，改为按图片真实比例设背景框：`.fig-row.three > .feature-figure img { aspect-ratio: 4/7 }`、`.fig-row.four > .feature-figure img { aspect-ratio: 4/5 }`，沿用 `object-fit:contain` + 等高对齐、全部留在 1200px 内容栏内，消除原 4:3 横框对竖图两侧的大量留白、视觉上更大更清楚。`two` 与 `single` 行维持原 4:3/原比例不动。改动已提交。
- **Git 打 `v1.0.8` 备份标签**：提交 `f8b7e53`，`git tag v1.0.8`。覆盖：觉醒系统页内容 + reveal 阈值修复(threshold 0) + 图床迁移(删本地图) + 全局 fig-row 对齐 + 基础改动三/四列竖图框比例修正(4:7 / 4:5)。本次 `git add -A` 把 `assets/导出网页/`（觉醒系统导出 HTML + media + katex 等 css 字体）一并纳入版本控制作参考源。标签链 `v1.0.1 → v1.0.8`，工作区 clean。仍仅本地无 remote。
- **【女巫】职业技能详情页（skills-sorceress.html）制作与修复**：基于 `assets/5.2 女巫（Sorceress）/.../5.2 女巫（Sorceress）.html`（wolai 导出，93 张 png）自动转换，转换器 `_build_sor.py`（DOMBuilder 混合内容 DOM + render_inline/list/table/figure/fig_row/nested_details/skill 系列）。图片本地化到 `images/skills-sorceress/`（站点无图床上传工具，本地图保可用）。4 章节 4.2.1~4.2.4、30 个技能 h4、每技能主图+觉醒面板并排 `fig-row.two`。**关键修复（_2 觉醒面板不渲染）**：① 原 `skill_figs` 只收「不在 details 内」的图，而 `_2` 面板在技能 `<details>` 内被排除——改为收全部图（跳过 `_1` 67px 图标），`name` 按 `alt.split()[0]` 归集，主图(orighinal_sor_N.png)排前、`_2` 排后；② 容器(div/p/section/article)带文字时原 `render_block` 直接返回 `<p>` 而丢弃其中 figure——改为始终递归子元素、直接文本单独成段；③ `render_inline` 丢弃 `figure` 避免冗余图注泄漏；④ 全局主图画廊 `div.wolai-simple-table` 整体跳过（图已在每技能渲染，否则重复/变空表）；⑤ 3 个 `_2` 面板 alt 用异名（寒冰掌握/静态立场/雷暴）对应 冰冷支配/静电立场/雷电风暴，用 ALIAS 合并进正名并删除异名键；⑥ `<details summary="backup">` 是 wolai 导出冗余块，整体跳过。最终结果：30 主图 + 30 `_2` 觉醒面板全渲染、0 空段、0 wolai 残留。改动未提交（待用户「备份」）。
